<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ClashSharp.Components.MasterModeButton"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ToggleButton
        MinHeight="78"
        HorizontalAlignment="Stretch"
        HorizontalContentAlignment="Stretch"
        IsChecked="{Binding IsChecked, ElementName=Root, Mode=OneWay}"
        Command="{Binding Command, ElementName=Root}">
        <Grid Padding="6" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <FontIcon
                Glyph="{Binding Glyph, ElementName=Root}"
                FontSize="20"
                Width="28"
                VerticalAlignment="Top"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

            <StackPanel Grid.Column="1" Spacing="3">
                <TextBlock Text="{Binding Title, ElementName=Root}" Style="{ThemeResource BodyStrongTextBlockStyle}" TextTrimming="CharacterEllipsis" />
                <TextBlock
                    Text="{Binding Description, ElementName=Root}"
                    TextWrapping="WrapWholeWords"
                    MaxLines="2"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </Grid>
    </ToggleButton>
</UserControl>

# 服务宿主退出、显式重启与会话隔离

服务进程退出后，原核心必须随其作业退出；新服务进程必须建立新会话，不能接受
旧会话的控制绑定。此次直接验证生产服务、真实核心和生产卸载路径，不需修改
产品代码。

## 候选与执行边界

生产 MSIX 和签名引擎沿用 `f1fca8eee0f4a7169c2ac98fde78e0a8f5872a13`，
分别为 SHA-256
`7d57b41bb13882eb3ab356f17ad7727675501d24590a1085d62547673c40c411`、
`cc4cbf12f4ccc82e7996772017d31f4d4ec0b02d03dd855827c0f0303740d02d`。
审阅版本及 IPC 客户端源码为 `185ded9471874c82c5bf0d8a9ac37e6792fd9c55`；
运行前逐项核对服务、Core、安装器、Watchdog、两份客户端源码及构建/输入脚本
相对候选没有改变。这不声明 M3i 新 MSIX 完成了同一原生测试。

探针仅能在匹配计划摘要、路径和 WDAG 身份的 Windows 11 客体执行；配置为
独立最小样本。开发机没有执行安装或探针，没有导入测试证书或改变代理。测试
信任和卷根 ACL 夹具限于断网 Sandbox，结束后恢复。只读输入及报告目录分别
映射，原生故障操作只针对该次服务的已核验进程句柄。

首次运行 `0576aec934f8409ab24cbb0a1b8f8888` 在核对宿主映像路径时失败：
夹具遗漏了 `current\Host` 中的 `Host` 子目录。拒绝发生在终止服务之前；普通
Stop 和九项兜底清理通过，Sandbox 已销毁。该运行不计入故障恢复成功。
原探针、候选及记录保持，修正路径后使用独立的新探针和候选目录。

最终探针 SHA-256 为
`dfe9bc676aeeb007eeb5b5064d3369c45caa9f70d80c0f68fadfdf19a6d7f45f`，
计划摘要为 `7563b250ea53325f35bab73326e7833b4eabe681f0c3aa9647d25971bea7e071`。

## 实际结果

成功运行 `9895fc4df28b4557b5fcc75625de412f` 于
2026-09-08 05:46:20–05:47:07 UTC 执行：

- 生产客户端在发送凭据前验证管道对应的 SCM 进程。首次启动为代际 1、Rule
  模式、TUN 开启、mixed-port 为 0；TCP 9090 和 10000 不监听。
- 核验并持有宿主 PID 7776 与核心 PID 8164 的句柄后终止宿主。两个旧句柄均
  确认退出，SCM 观察到 Stopped。探针显式调用 StartService，并等待实际 Running
  及新 Hello；新宿主为 PID 7404，Hello 返回新会话和 Stopped，代际与摘要为空。
- 再次 Start 创建核心 PID 7228，代际号和配置摘要与旧会话相同。旧会话绑定
  返回 `service.controller.stale_generation` 且没有有效配置，新会话绑定成功查询
  Rule/TUN 状态。这单独证明会话标识参与验证，不依赖代际号或摘要差异。
- 成功路径不发送 Stop。生产卸载 PID 8068 接管仍在运行的核心并确认其退出，
  返回 `installer.completed`，没有待恢复事务。05:47:05.7524226 UTC 的产品快照
  在兜底清理之前已确认包、服务、两类证书、机器载荷、服务数据和事务文件缺席。

八步 IPC 均符合预期，源配置摘要保持。SCM 仅记录一次与注入时间相差约 16 ms
的预期 7034 事件；没有其他 1026、7031 或服务宿主失败事件。九项客体清理均
通过，Sandbox `5130b94f-d0ff-4170-ac9e-44210ae680ae` 已销毁，输入与开发机
代理未变。收据为 `artifacts/verification/installer-service-host-recovery-m5f.json`，
其中探针报告摘要为
`d2105cecffdabb43f8ebae899ede2e15afdf14d61baab0445710c88308b3c7eb`。

此次 SCM 重启由探针显式发起，不能据此声称服务故障自动恢复通过。原生 API
结果也不代替 WPF/UAC 交互、跨账户界面、系统断电或完整发布矩阵验收。

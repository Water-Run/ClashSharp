# 首页磁贴扩充与专项验收

用户要求磁贴作为核心卖点，信息至少覆盖 Clash Verge 类客户端，且必须在目标 Windows Server 上验证实际功能、外观和交互。此项属于原有完整验收的追加范围；单元测试和源码检查不能替代实机通过。

## 对照范围

对照 [Clash Verge Rev 首页官方资源](https://github.com/clash-verge-rev/clash-verge-rev/blob/dev/src/locales/zh/home.json)中的九类首页卡片。核对日期：2026-09-23。实现保留 ClashSharp 的自定义磁贴及四种运行模式。

| 信息领域 | 对应实现与待验收内容 |
| --- | --- |
| 订阅与配置 | 活动配置名称、节点和规则数、订阅用量/额度、到期时间、最后更新、自动更新周期；本地配置、缺失元数据、零额度、无限期、过期和超额分别检查 |
| 当前代理 | 当前节点、延迟、模式；无节点、未测、成功、超时、失败和切换后的更新 |
| 网络设置 | 系统代理状态及实际地址、TUN 期望状态及生效状态、混合端口、内核运行归属 |
| 流量 | 上传/下载速度、最近 60 次采样趋势、活动连接数、本次会话累计、历史累计；停止/重启、短连接和采样间隔检查 |
| 网站检测 | 首页直接执行三个已配置目标，显示 HTTP 状态码、响应耗时、超时/连接失败；走当前 Windows 系统代理和 TUN 路由 |
| IP 信息 | 用户点击后通过当前网络查询 IP、地区、ASN、提供方、组织、时区、检测时间与来源；不自动请求第三方 |
| 内核信息 | 版本、内核进程内存、真实启动时间与持续运行时间、规则数量、运行归属及服务状态 |
| 系统信息 | 操作系统与架构、应用架构/版本、开机启动、启动/关闭行为、手动检查更新的结果与时间 |
| 定制与交互 | 推荐 20 张磁贴，保留已有自定义布局；选择、搜索、恢复推荐、取消、拖动、重启保存、完整详情和键盘操作 |

IP 查询使用 [ipwho.is 官方接口](https://ipwhois.io/documentation)，请求设置超时和响应体大小上限；只保留当前页面的检测结果。路由上下文变化会清除旧结果，进行中的旧请求不能回填到新上下文。它描述的是到该检测服务的实际出口，不把 TUN 下的普通请求错误称为强制直连。

内核进程内存来自已核实归属的应用子进程或服务监管子进程。[mihomo 的连接快照](https://github.com/MetaCubeX/mihomo/blob/Meta/tunnel/statistic/manager.go)包含缓存内存字段；为避免尚未刷新的缓存值，磁贴读取被监管进程的工作集。诊断字段可缺失，不改变内核归属判定。完整内核启动时间随服务连接快照传回，配置重载不应重置运行时间。

## 专项验收要求

1. 每一张可选磁贴：标题、图标、说明、数值来源、完整详情及其可用状态。无命令的磁贴支持鼠标、Tab/Enter/Space 展开可选中文本。
2. 四种模式以及应用/服务归属：数值与实际服务、进程、端口和系统代理一致；期望状态和当前状态不能混淆。
3. 动态数据：真实上下行、短连接、无流量、停止、重启、切换节点/配置、返回首页；趋势不跨长时间缺口连线，数值不会溢出。
4. 订阅：本地/内置配置、完整/部分/缺失元数据、零额度、无限期/过期、更新失败与重试；不凭空补零。
5. 网站/IP：未检测、进行中、成功、HTTP 错误、网络失败、超时、取消、再次执行、检测中离页或切换路由；失败不能继续显示上次成功结果。
6. 布局编辑：单选增减、隐藏全部、搜索无结果、恢复推荐后保存/取消、拖动排序、拖动不触发命令、重启保留、旧布局升级。
7. 显示：800×600、恢复窗口、最大化、浅/深/系统主题、七种语言与波斯语 RTL；长名称、长地址、长说明和大数值可查看完整内容。
8. 快捷操作：TUN、开机启动、采样等开关的忙碌反馈、失败恢复、重复点击；延迟、导入、导出与网络检测的取消和重试。

## 当前状态

2026-09-23：可选磁贴从 58 张扩展到 68 张，推荐展示 20 张。当前服务器安装 `3e30bec`，通过正常卸载后重新安装及载荷散列核验；验收数据另行恢复，不计自动升级数据保留。原 141 项矩阵仍为 68 通过、31 部分完成、42 待测。下文按时间记录历次候选；旧候选的待复测事项以较新的具体证据为准。

13:20–13:34 UTC 的首轮磁贴实测中，推荐 20 张正常出现，信息磁贴点击可展开详情。公网 IP 查询返回完整详情；网站检测区分两项超时与一项 HTTP 200 成功。切换模式后两类检测结果清空为未检测。待机模式显示约 26.5 MB 内存和真实运行时长；规则接管后，服务会话中的新内核显示约 56.3 MB，启动时间 21:33:20，与 SSH 读取的进程信息一致。本地配置的订阅用量和到期磁贴均显示 Local profile，没有虚构配额。

同时发现四个前端问题：编辑对话框在最大化时仍裁掉恢复推荐入口；磁贴主体拖动未排序；内核磁贴在停止或运行时均显示 Ready；公网 IP 的多行详情挤掉主数值。源码已调整编辑列表高度、增加独立拖动手柄及键盘焦点路径、显示实际内核运行状态、限制摘要为一行并保留完整详情。这些修复仍待下一安装包复测，键盘、拖动保存、最小窗口、七语及 RTL 尚未标记通过。

本次修复的 18 项目构建零警告零错误（27.74 秒），主程序完整回归 **3,165 / 3,165**，无跳过（1 分 12 秒），完整格式检查通过。逐磁贴证据保存在 `dashboard-tile-matrix.json`；一次成功查询不代表取消、重试、失败和路由变化的全部场景均已验收。

13:39 UTC 通过真实编辑器新增运行归属磁贴，保存后首页显示 Windows service，与会话 0 的内核对应（`133946Z-202227205`）。13:43 UTC 新建并更新受控订阅、设为当前配置，首页显示 3 KB / 1 GB、2030/1/1 8:00 到期和本次更新时间；详情可以展开（`134359Z-786710807`、`134416Z-418653584`）。这些是完整元数据和本地配置两个分支的实际观察，零额度、过期及部分缺失等仍待测。

停止内核后离开首页再返回，发现本次会话流量归零（同上截图）；应用级采样器仍保留累计量，但新页面的失败回退遮盖了它。现合并共享累计值与页面实时采样，保留累计量，同时不恢复过时的速率、连接数、内存和启动时间。新增新页面/停止内核/随后状态不可用的回归测试，18 项目构建零警告零错误（47.77 秒），主程序 **3,166 / 3,166**（1 分 19 秒，无跳过），完整格式检查通过。此修复晚于 ef6bfa7 打包，尚未安装验证。

13:56–14:11 UTC 在 ef6bfa7 上复测：恢复窗口、最小 800×600 外框、最大化窗口中的磁贴编辑器均完整显示恢复推荐及保存/取消按钮（`135632Z-483355834`、`140919Z-175553066`、`141050Z-458256511`）。点击恢复推荐再取消保留原先自定义选择；搜索无结果、清除搜索、Esc 取消及返回编辑入口焦点正常。拖动独立手柄把内核移到第三位，没有触发详情；离开再回首页顺序保留（`140025Z-9265598`、`140539Z-675513813`），重启保存尚待测。

键盘复测确认 Enter 可打开首张信息磁贴，但下一个 Tab 跳出整个区域（`135900Z-230208984`、`135936Z-260014326`）。后续候选显式设置模板控件的 TabNavigation，并去掉网格自身的空焦点；提示浮层改用编译绑定，避免跨弹出层名称域依赖。构建零警告零错误（43.34 秒），完整格式检查通过；实际键盘遍历和悬浮提示须在新候选继续验证，不能由 XAML 检查推定通过。

相关资源、布局与主控视图模型回归 **172 / 172**，无跳过。第一次检查发现旧资源断言仍要求 ElementName 绑定，更新为编译绑定后重跑通过。

14:18–14:30 UTC 的 ef6bfa7 原生复测覆盖订阅零值、部分缺失、非法负数/溢出、过期与超额：零额度显示 0 B / 0 B，零到期显示无限期；缺失或无效字段显示未提供；过期日期及 3 MB / 1 MB 超额用量没有被截断或回绕。正常退出并重新启动后，内核磁贴仍排第三，拖动排序的进程重启保存通过（`143000Z-906606429`）。

这轮也发现订阅摘要缺少剩余额度、超额和过期提示，部分元数据中已知的单向流量无法查看。现保留总用量与日期，并在摘要/完整详情加入剩余或超额数量、分别上传/下载、剩余天数或已过期，七种语言同步补全。余额用 decimal 计算，零额度不推定为无限；部分缺失不补零，未来不足一天计为一天。18 项目构建零警告零错误（34.94 秒），主程序完整回归 **3,170 / 3,170**（1 分 9 秒，无跳过），完整格式检查通过；新增详情尚待打包后的实机验收。

14:32–14:38 UTC 使用 227cb08 管理程序正常卸载、重新安装。卸载后应用包/服务均为零、六处自有目录不存在，共享运行库保留；安装后应用仅注册给验收账户，DLL 与候选载荷 SHA-256 完全相同（`A979C594421E3DFC252CCEE3F45B507E6011D79213A26777EAF085CDD92FCA77`）。验收数据另由 14:31 的闭合备份恢复，25 个文件逐一核验散列；此操作不计自动升级数据保留。

14:41–14:46 UTC 在 227cb08 完成通用键盘路径复测：Tab 从编辑入口进入上传、下载磁贴，继续跨行自动滚动，能到第 21 张运行归属磁贴并离开区域；Shift+Tab 反向移动，Enter/空格打开完整详情，Esc 返回原磁贴。提示浮层显示数值、详情及说明（`144143Z-932584336`、`144206Z-159746320`、`144421Z-991521920`、`144508Z-181600146`、`144524Z-63913945`、`144604Z-413002423`、`144644Z-866207126`）。此项证明通用键盘路径修复，不替代每个快捷命令的独立验收。

14:47–14:52 UTC：待机启动后内核磁贴显示 Running；实际进程 7536 位于会话 2，混合端口 10000 归属一致。受控 HTTP 下载 245,760 字节期间，磁贴显示约 4 KB/s 和一条活动连接。完成后控制器读数为上传 100、下载 246,441 字节（含协议开销），磁贴显示合计 240.76 KB，活动连接及速率归零（`144948Z-591355506`）。禁用、离开并返回首页后保留相同累计量，内核/内存/运行时长显示停止或不可用；SSH 确认内核进程与端口均已消失（`145157Z-154332979`）。14:54 UTC 从托盘安全退出完成。本次会话流量跨页面保留修复通过，跨内核再次启动的累计仍待测。

15:00–15:15 UTC 在 3d5bad4 验证新增订阅详情：超额显示 Quota exceeded by 2 MB，详情保留上传 2 MB / 下载 1 MB（`150020Z-482714901`）；2021 年到期显示 Expired（`150244Z-632438312`）；仅上传 999 B 时分别显示已知上传和未知下载，清除旧余额及日期（`150432Z-650154470`）；无元数据时全部显示 Not provided（`151045Z-521813506`）。恢复完整元数据后显示 3 KB / 1 GB、余额 1024 MB（格式化舍入），2030/1/1 8:00、剩余 1196 天（`151514Z-153397804`）。这些分支均通过实际订阅更新和首页回读完成。

15:16–15:21 UTC 验证深色主页和编辑器、切换波斯语并通过设置页正常重启。波斯语主页按 RTL 排列，Tab 按阅读顺序移动、跨行自动滚动，Enter 详情与 Esc 返回正常（`151914Z-358210044`、`152029Z-876136007`、`152145Z-707868262`）。但编辑对话框仍为 LTR（`151832Z-49477893`）；已修复 ThemedContentDialog 继承窗口方向。磁贴编辑另增加全部显示、全部隐藏与已选数量，覆盖搜索之外的完整列表，保存才提交草稿，调整列表可用高度以容纳操作行。七语文案齐全；18 项目构建零警告零错误（34.19 秒），相关回归 537 / 537，无跳过，完整格式检查通过。此项新 UI 待下一候选实机验证，不能算 RTL 全部通过。

15:23–15:29 UTC 在 3d5bad4 波斯语深色界面切换规则接管，内核磁贴显示运行，内存 55.04 MB、启动 23:23:49、运行 32 秒（`152422Z-532983066`）；SSH 证实进程 15760 位于会话 0，启动 23:23:49.512，工作集 57,708,544 字节，服务 Running，后续归属磁贴显示 Windows service（`152458Z-966055532`）。公网 IP 主数值可见，详情完整（`152525Z-925588421`、`152538Z-53396615`），多行摘要遮挡主值修复通过；网站检测显示 2/3、两个 HTTP 200 及一个失败（`152609Z-843969562`）。禁用后实时值清零、内存/运行时长不可用，会话保留 4.61 MB（`152825Z-123384440`）。设置页正常关闭后，29 个验收状态文件完成闭合备份；取消检测、所有语言和每项命令仍未全测。

15:34–15:38 UTC 在 3e30bec 验证波斯语深色磁贴编辑器：标题、复选框和按钮采用 RTL（`153406Z-797716899`）。全部隐藏后取消保留原 21 项；无搜索结果时全部隐藏仍覆盖完整目录，计数变为 0，保存后首页为空但保留编辑入口。设置页正常重启后空布局仍保留（`153736Z-249167438`）。启动引导框另继承 ContentDialog，仍为 LTR；现把方向继承补到统一 WindowDialogCoordinator，待下包原生复测。

15:47–15:53 UTC 将外框缩到 800×600，编辑器的搜索、批量按钮、计数、恢复推荐、保存和取消完整可见（`154731Z-696719861`）。全部显示为 68，手动取消/重选一项为 67/68（`154757Z-941311906`、`154826Z-629820917`）；但保存失败且原布局未变，重开回到 0（`154919Z-584591720`）。根因为设置归一化仍限制 64 项。上限改为 128，并添加实际完整磁贴目录通过设置校验、128/129 边界回归，保留非法标识符和过量导入拒绝。18 项目构建零警告零错误（38.92 秒），405 项相关回归全部通过、无跳过。全部显示保存仍须新包复验。恢复推荐 20 项并保存成功，重开仍为 20（`155035Z-260233998`、`155232Z-492707820`）。

16:03–16:05 UTC 在 3e30bec 把网站检测第一地址暂改为仅本机的延迟响应测试端点。真实界面显示第一目标超时，另外两个目标 HTTP 200；重试能再次完成。取消操作到达时检测已结束，因此取消仍记未验证，不能算通过；该测试地址待恢复。

16:13–16:16 UTC 在 0492ead 上保存全部 68 项，重开编辑器为 68/68（`161351Z-212431407`、`161408Z-624647291`）；通过设置页正常重启后仍为 68/68（`161600Z-724590062`），完整目录保存限制修复通过。波斯语启动引导标题、内容及状态图标均为 RTL，但深色模式出现浅色遮罩、旧方角模板（`161523Z-898701944`）。按 [WinUI 派生对话框样式说明](https://learn.microsoft.com/en-us/windows/windows-app-sdk/api/winrt/microsoft.ui.xaml.controls.contentdialog#derived-controls-with-winui-styles) 为组件补上继承 DefaultContentDialogStyle 的样式，18 项目构建零警告零错误（34.91 秒），相关现有回归 118 / 118，无跳过；样式修复等待下一候选原生复测。

16:17 UTC 完整 68 项布局通过 Tab 到达最后一张磁贴，Enter 打开详情、Esc 返回，随后 Tab 离开磁贴区域（截图 161713Z-301132637、161733Z-62055762）。同时发现最后几项统计磁贴复用了页面标题，节点的两个数字没有含义说明；现改为专门的系统日志、历史连接、流量采样和节点健康记录标题与说明，明确共享日志数据库大小及节点流量记录，七语同步。构建零警告零错误（21.23 秒），496 项相关回归通过、无跳过；格式检查退出 0，有工作区加载警告。逐命令动作和新文案原生复验仍待完成。

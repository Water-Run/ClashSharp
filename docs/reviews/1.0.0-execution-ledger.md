# ClashSharp 1.0.0 开发执行账本

目标：完成 1.0.0 的主体、WinUI 3 页面与磁贴、安装器及可追溯验收。应用语义版本为 `1.0.0`，程序集和 MSIX 版本为 `1.0.0.0`。

执行约束：保留开发机当前代理；真实接管、服务、证书、安装和卸载测试使用远程隔离环境。用户现有 Clash 配置只进入被 Git 忽略的私有测试输入，不进入代码、日志、截图或发布资产。关键里程碑通过对应验证后提交并推送 `main`。

| 里程碑 | 范围与验收 | 状态 |
|---|---|---|
| M1 | 同步 main；订阅锁回收、PowerShell AST、完整 WinUI format 门禁；3231 项测试 | 已推送 `16599a5`；[对应远端 CI 通过](https://github.com/Water-Run/ClashSharp/actions/runs/34041537649) |
| M2 | 页面读/写生命周期、顺序与取消；主控磁贴原生键盘和无障碍；1.0.0 版本统一 | 已推送 `6eccb93`；[远端 CI 通过](https://github.com/Water-Run/ClashSharp/actions/runs/34043293313)；实际键盘、拖动和 UIA 验收归入 M5 |
| M3 | 主控功能磁贴的完整异步交互、数据页面状态、设置事务及日志边界 | M3m 将采样开关、间隔和循环生命周期统一到应用层，对应 CI 共 4686 项通过；完整设置代际迁移和实际页面验收继续推进 |
| M4 | 固定发布输入、完整 Installer 安装/修复/升级/卸载与换绑、候选构建 | WPF 单页、Rust 清除、绿色入口、账户迁移和旧账户卸载已连接；机器与用户证书独立管理，正式签名构建已连接生产运行时；完整发布验收继续推进 |
| M5 | 用现有配置验证真实节点、代理/TUN、恢复、窗口与所有页面；签名候选绑定的故障矩阵 | 原配置 32/32 节点、实际 HTTP 代理/TUN 通过；Windows 11 包安装、WinUI 窗口和部分磁贴操作已有证据，机器证书 20 进程/138 断言通过；服务装配修复后的候选完成正常三项事务和八项中断/占用恢复，完整发布验收继续推进 |

远程宿主为 Windows Server 2025 x64，具有 Desktop Experience、Git 和 PowerShell 7，仍用于绿色入口校验；私有 .NET SDK 10.0.201 已验证，没有修改全局 SDK 或 PATH。当前已使用本机现有 Windows Sandbox 提供独立 Windows 11 客体，完成实际包、窗口及证书能力验证，开发机代理保持。正式安装器仍限定 Windows 11 客户端；正常 WPF 安装及完整机器故障矩阵尚未完成，不能由组件探针结果推定。

## M3n 设置代际切换开发分支（2026-09-08）

- `feat/settings-generation` 已实现规范化旧偏好迁移、异步设置会话、Running/Failed/verified 流转、启动重新观察及代际内服务解析。隔离损坏文件后的错误初始化先由两项回归复现，再修复。
- 新增 84 项回归；主程序 2705 项全部通过，零失败、零跳过。18 项目 Release x64 构建零警告、零错误；完整 format 检查 1459 个文件、0 处变更。收据为 `local-validation-settings-generation-foundation.json`。
- 基础提交 `2775afe` 已推送开发分支并建立[草稿 PR #5](https://github.com/Water-Run/ClashSharp/pull/5)，[两项 CI 成功](https://github.com/Water-Run/ClashSharp/actions/runs/34211372829)，实际四份 TRX 共 4770 项通过，新增 84 项均执行。合并提交 `8febfaf` 的 tree 与该基础提交相同，收据为 `ci-validation-settings-generation-foundation.json`。
- 公共异步入口继续持有同一个代际，覆盖 desired 提交、全部受影响批次及验证；修复提交后开始退出排空会截断后续应用的衔接问题。追加 9 项回归，完整主程序 2714 项通过，构建零警告、零错误，format 检查 1464 个文件、0 处变更；本分支累计新增 93 项，收据为 `local-validation-settings-generation-facade.json`。
- 公共入口提交 `a08fc4b` 的[两项 CI 成功](https://github.com/Water-Run/ClashSharp/actions/runs/34214328575)，实际四份 TRX 共 4779 项通过，零失败、零跳过；收据为 `ci-validation-settings-generation-facade.json`。
- StartupTask 和 Sampling 适配器使用生产服务进行应用与独立观察；采样循环持有已安装的间隔，并串行完成旧任务排空和新配置安装。修复启动探测吞掉嵌套致命异常、设置入口包装该异常的问题，两项回归先红后绿。追加 16 项回归后，完整主程序 2730 项通过，构建零警告、零错误，format 检查 1473 个文件、0 处变更；累计新增 109 项，收据为 `local-validation-settings-generation-runtime.json`。平台边界采用隔离模拟，尚未装配进生产设置权威。
- 运行时适配提交 `37f4c51` 的[两项 CI 成功](https://github.com/Water-Run/ClashSharp/actions/runs/34217135381)，实际四份 TRX 共 4795 项通过、零失败、零跳过；收据为 `ci-validation-settings-generation-runtime.json`。
- 控制端凭据已从偏好及核心配置设置端口中移出，生产启动、控制端请求、配置生成和全数据清理改用独立凭据服务。写入、删除均独立重读确认；存储不可用阻止启动，普通偏好重置保留凭据，终态清理使用专门维护许可。30 项新用例替换一个旧设置类职责测试后净增 29 项，完整主程序 2759 项通过，构建零警告、零错误，format 检查 1488 个文件、0 处变更；收据为 `local-validation-controller-credentials.json`。Windows 存储边界使用隔离属性集测试，新的实际打包候选启动验收待 CI 包产出。
- 凭据拆分提交 `e9026f8` 的[两项 CI 成功](https://github.com/Water-Run/ClashSharp/actions/runs/34221242635)，实际四份 TRX 共 4824 项通过，30 项新凭据用例全部实际执行；收据为 `ci-validation-controller-credentials.json`。已补充 Sandbox 启动完成状态检查，避免把持续显示的启动错误页算作成功；PowerShell 两版本各通过 111 项报告断言及 16 项隔离 SQLite 断言。新候选的实际运行证据仍待取得。
- 生产消费者和 profile/log/trigger 生命周期尚未切换，临时单一设置权威门禁保留。完整接入及验收继续在开发分支完成；实现、验证边界和剩余依赖见[设置代际切换](../design/2026-09-08-settings-generation-cutover.md)。
- main 的证据提交 `e3f597c` 两项 CI 均成功；实际四份 TRX 共 4686 项通过，零失败、零跳过，收据为 `ci-validation-m3m-docs.json`。本次同步确认 origin/main 仍为该提交。

## M3m 连接采样设置统一事务（2026-09-08）

- 修复页面提前保存采样设置及失败覆盖后端结果的问题，四项回归先复现后通过。页面只暂存完整选择，应用后整组读取权威状态；连续请求和命令完成间隙的请求均由重置前排空等待。
- 设置页、应用动作和触发器共用应用层协调器，先暂停并排空循环，再批量提交开关/间隔并验证激活；失败分别恢复完整偏好和原实际运行状态。取消不会中断已开始的提交或补偿；无法恢复时保留完整诊断。
- 新增 44 项回归，全部 2621 项主程序测试通过，零失败、零跳过；18 项目 Release x64 构建零警告、零错误，完整 format 检查 1439 个文件、0 处变更。收据为 `local-validation-m3m.json`，范围见[连接采样统一事务](../design/2026-09-08-connection-sampling-settings-coordinator.md)。
- 真实监督循环使用模拟采样源和存储，设置确认在内存回退中；没有修改开发机代理。生产仍使用唯一 LocalSettings authority，完整代际迁移继续推进。
- `2e5a4a4` 的[两项 CI 均成功](https://github.com/Water-Run/ClashSharp/actions/runs/34203924288)，四份实际 TRX 共 4686 项通过、0 失败、0 跳过，包含新增 44 项；Core 行覆盖率 93.53%、分支覆盖率 86.73%。收据为 `ci-validation-m3m.json` 和 `ci-sampling-coordinator-regressions-m3m.json`。
- CI 开发归档 `10047237419` 共 317451347 字节，SHA-256 为 `c5f802bac8c3f0229acc2b8b628c4e2309b9371e33822b3e93a1e64a328a6224`；EXE 为 `a6a0cec671610a200a209df1437585ce4e492d74f09ba50178ed438c8d7a1ba3`，MSIX 为 `2fdc848edd21f84ce71d1731681ebfd61f05c3a051b50758cdca016672e63f1f`。八个文件与包内第三方声明匹配；远端无全局 .NET 的绿色校验通过，临时目录清理且无残留进程，没有正常安装、证书或代理修改。收据为 `installer-green-validation-m3m.json`。

## M5h 采样事务提交的包启动（2026-09-08）

- M3m 同一实际 CI MSIX 在断网 Windows 11 Sandbox 安装后显示主窗口并持续观察 30037 ms。运行 `4c3f35af186d4550930ade3f75bba950` 的 12 步和七项清理通过，Sandbox 已销毁、输入和宿主代理未变。
- 收据为 `sandbox-package-validation-m5h.json`，摘要和范围见[包启动验证](../design/2026-09-08-package-launch-validation.md#m5h-采样事务提交的包启动)。本次未执行普通 WPF 安装、完整页面操作和正常退出，旧候选的服务/卸载矩阵保持独立。

## M3l 开机启动设置统一事务（2026-09-08）

- 修复页面先保存启动开关、后端因此读取错误补偿基线，以及页面覆盖后端结果的问题；四项回归先复现后通过。页面现在只暂存选择，应用结束后读取权威设置；连续请求及命令完成间隙的请求在重置前排空。
- 新增应用层 StartupSettingsCoordinator，设置页、共享应用动作与触发器使用同一个串行实例。保存前验证真实注册，失败分别恢复原偏好和实际注册；修改开始后不因页面取消中断，恢复失败保留完整诊断图。
- 新增 47 项回归，全部 2577 项主程序测试通过、0 失败、0 跳过；18 项目 Release x64 构建 0 警告、0 错误，完整 format 检查 1431 个文件、0 处变更。收据为 `local-validation-m3l.json`，设计与验证范围见[开机启动统一事务](../design/2026-09-08-startup-settings-coordinator.md)。
- `e552479` 的 CI 34197887559 打包成功，主程序有 1 项退出交接夹具清理失败，后续 Installer 测试未执行；失败实际报告已保留。该夹具现使用共同取消期限并等待全部数据库使用者结束，本地和新 CI 回归均通过。
- `110f62e` 的[两项 CI 均成功](https://github.com/Water-Run/ClashSharp/actions/runs/34200577217)，四份实际 TRX 共 4642 项通过、0 失败、0 跳过，包含新增 47 项；退出交接用例 235.1257 ms 通过。Core 行覆盖率 93.53%、分支覆盖率 86.73%；收据为 `ci-validation-m3l.json` 和 `ci-startup-coordinator-regressions-m3l.json`。
- CI 开发归档 `10045890331` 共 317435295 字节，SHA-256 为 `97fe3255a2d6985746a26c948214b69cc7758b398dde2a6ddcd680ff6f1e4420`；EXE 为 `5e67339e02d34646c385030824f8a851c16bae0f7c2b90083e9c2e8b7976f6c4`，MSIX 为 `a3226e58042caca966b32201c273671d039646e393ca62b9666c99fc158773e2`。八个文件及包内第三方声明匹配，远端无全局 .NET 的绿色校验通过，临时目录清理且无残留进程；没有正常安装、证书或代理修改。收据为 `installer-green-validation-m3l.json`。
- 本轮平台注册为模拟，设置仍使用唯一 LocalSettings authority；没有修改开发机代理、开机启动或安装状态。完整设置代际迁移与真实页面验收继续推进。
- 证据提交 `9e75fb4` 的[两项 CI 也均成功](https://github.com/Water-Run/ClashSharp/actions/runs/34202217324)，四份实际 TRX 共 4642 项通过、0 失败、0 跳过；收据为 `ci-validation-m3l-docs.json`。该文档提交已同步到远端干净 main。

## M5g 最新开发包启动（2026-09-08）

- 直接使用 M3l 提交 `110f62e` 的实际 CI MSIX，在断网 Windows 11 Sandbox 安装依赖、开发公钥证书及包；核对版本、包身份和进程 EXE 摘要后，主窗口持续观察 30175 ms。
- 运行 `41c2f6377fd24826a4b0e1766c750b5f` 的 12 步及七项客体清理全部通过，宿主确认 Sandbox 已销毁、输入与代理未变。收据为 `sandbox-package-validation-m5g.json`，候选摘要和范围见[包启动验证](../design/2026-09-08-package-launch-validation.md)。
- 该组件验证没有执行普通 WPF 安装、完整页面操作或应用正常退出；旧候选的服务与卸载矩阵仍按原候选独立记录。

## M3k 备份导入应用层协调器（2026-09-08）

- 主控磁贴与设置页共用的备份导入，其保留提交、配置缓存失效、运行时应用和失败补偿移到 Application 层。与重置共用运行时参与者和不可变快照，页面只连接适配器、持有独占许可并处理恢复重启。
- 回滚后先确认完整旧设置已恢复，再应用旧运行时；不完整回滚、存储读取及补偿失败保持明确恢复分类。提交/回滚清理只重试同一决定，收据释放失败保留原始错误图，持久开始后的取消不截断事务。
- 新增 38 项回归及全部 2530 项主程序测试通过，零失败、零跳过。Release x64 完整构建零警告、零错误，完整 format 检查 1424 个文件、0 处变更。收据为 `local-validation-m3k.json` 和 `1.0.0-m3k-main.trx`。
- 生产存储仍为唯一 LocalSettings authority；运行时端口测试为模拟，本次未修改开发机代理或安装状态。设计见[备份导入协调器](../design/2026-09-08-settings-import-coordinator.md)，完整设置代际迁移与实际页面验收继续推进。
- `72f698d` 的[两项 CI 均成功](https://github.com/Water-Run/ClashSharp/actions/runs/34196643675)，四份实际 TRX 共 4595 项通过、0 失败、0 跳过，包含上述 38 项回归。Core 行覆盖率 93.53%、分支覆盖率 86.73%；收据为 `ci-validation-m3k.json` 和 `ci-import-coordinator-regressions-m3k.json`。
- CI 开发归档 `10044389965` 共 317417328 字节，SHA-256 为 `a076b94f4a0a13406246666cb45cb82f476bac00d276f7b897214c5384b08624`；EXE 为 `509523f9ed921af24bd29bd85fbd9ee39815453371790e3abf612f57bbcf88ea`，MSIX 为 `4e621b6098cdb169c487468cfe197fcc77fd7c17a51945165b66f299b610a92a`。八个文件及包内第三方声明逐项核对，远端无全局 .NET 的绿色校验通过，临时目录清理且无残留进程；没有正常安装、证书或代理修改。收据为 `installer-green-validation-m3k.json`。

## M3j 设置重置应用层协调器（2026-09-08）

- 全量、启动组和网络组的参与者选择、收据提交/回滚、补偿与清理移到 Application 层；ViewModel 只保留页面操作排空、独占许可、状态投影和恢复重启请求。协调器使用明确端口和不可变运行时快照，不引用具体存储或页面实现。
- 修复状态观察者抛出异常时跳过已选定提交的问题，两项回归先复现、后通过；提交清理仍等待完成，并保留通知与清理两类故障。补齐权威设置读取失败的回滚/恢复处理，收据释放失败继续保留恢复分类和原始错误图。
- 新增 44 项回归及全部 2492 项主程序测试通过，零失败、零跳过。Release x64 完整构建零警告、零错误，完整 format 检查 1416 个文件、0 处变更。收据为 `local-validation-m3j.json` 和 `1.0.0-m3j-disposal-main.trx`。
- 生产存储仍为唯一 LocalSettings authority，运行时端口测试为模拟；没有修改开发机代理。设计和边界见[设置重置协调器](../design/2026-09-08-settings-reset-coordinator.md)，完整设置代际迁移和实际页面验收继续推进。
- `cbb6aa6` 的[两项 CI 均成功](https://github.com/Water-Run/ClashSharp/actions/runs/34194512912)，四份实际 TRX 共 4557 项通过、0 失败、0 跳过，包含上述 44 项回归。Core 行覆盖率 93.53%、分支覆盖率 86.73%；收据为 `ci-validation-m3j.json` 和 `ci-reset-coordinator-regressions-m3j.json`。
- CI 开发归档 `10043597266` 共 317405033 字节，SHA-256 为 `c7d4f298426881529159695de958118a1ab8cac14716777d6477894dd41e10d4`；EXE 为 `a7075b543c3a8c43fb4244a2bcc0d426625b1d678a9e68fc54759f358ea6e2e3`，MSIX 为 `d6c17078af1eee7f18fe3b234ece8ed89ccdfea746855916ac8eb245498b8bef`。八个文件及包内第三方声明逐项核对，远端无全局 .NET 的绿色校验通过，临时目录清理且无残留进程；没有正常安装、证书或代理修改。收据为 `installer-green-validation-m3j.json`。
- 证据提交 `78af14b` 的[两项 CI 也均成功](https://github.com/Water-Run/ClashSharp/actions/runs/34195566839)，四份实际 TRX 共 4557 项通过、0 失败、0 跳过，收据为 `ci-validation-m3j-docs.json`。文档提交已同步到远端干净 main。

## M3i 网络组重置事务（2026-09-08）

- 代理和透明代理组改为页面等待的保留事务，与启动组、全量重置共享排空和独占许可。代理组在一个批次中提交八项设置，透明代理组只修改 TUN；TUN 默认值受已确认的服务能力限制，并写入目标快照供重放。
- 代理组协调采样和网络，透明代理组只协调网络；失败时回滚完整存储快照并补偿选中参与者。事务开始后等待应用和补偿结束，页面最后一次发布完整字段，其他组的重启提示保持。
- 新增 37 项回归及全部 2448 项主程序测试通过，零失败、零跳过。Release x64 完整构建零警告、零错误，完整 format 检查 1408 个文件、0 处变更。收据为 `local-validation-m3i.json` 和 `1.0.0-m3i-main.trx`。
- 运行时回调为模拟，六项持久阶段中断使用新服务实例重放；本次没有修改开发机代理和路由。实现及范围见[网络组保留事务](../design/2026-09-08-network-group-reset.md)，完整设置代际迁移和实际页面验收继续推进。
- `185ded9` 的[两项 CI 均成功](https://github.com/Water-Run/ClashSharp/actions/runs/34190645348)，四份实际 TRX 共 4513 项通过、0 失败、0 跳过，包含上述 37 项回归。Core 行覆盖率 93.53%、分支覆盖率 86.73%；收据为 `ci-validation-m3i.json` 和 `ci-network-reset-regressions-m3i.json`。
- CI 开发归档 `10042218683` 共 317361404 字节，SHA-256 为 `6a1b86f3808e9c6b872feca2ecddfc9f6538abbaa235dd6a716796be95f321c5`；EXE 为 `026ef80874198368723993c19e54f67494bdd327aafd8eaab9113a35b99c0c49`，MSIX 为 `bd29f2976672121e9946d25811b7cb6e2403ba324dfcad8b04db9143dadbe244`。八个文件及包内第三方声明逐项核对，远端仓库同步且干净；该 EXE 通过无全局 .NET 的绿色校验，临时目录清理且无残留进程。没有正常安装、证书或代理修改；收据为 `installer-green-validation-m3i.json`。

## M5f 服务宿主退出与会话隔离（2026-09-08）

- 在断网 Windows 11 Sandbox 中准确终止持有句柄且经 SCM 身份核验的生产服务宿主；原核心随后退出。通过原生 API 显式重新启动服务，新 Hello 返回不同会话及 Stopped，没有继承旧运行代际。
- 新会话再次启动相同代际号和配置摘要；只保留旧会话标识的查询返回 `service.controller.stale_generation`，新绑定成功读取真实 Rule/TUN 配置。随后将核心保持运行交给生产卸载，持有的核心句柄确认退出，产品缺席快照先于兜底清理。
- 成功运行 `9895fc4df28b4557b5fcc75625de412f` 的八步 IPC 和九项客体清理通过。仅出现一次与注入时间匹配的预期 SCM 7034；没有其他服务宿主失败事件。Sandbox 已销毁、输入和主机代理未变；收据为 `installer-service-host-recovery-m5f.json`。
- 生产 MSIX/引擎候选仍为 `f1fca8e`，使用 `185ded9` 的未变客户端源码，并逐项确认服务、安装器及输入脚本源码等价。它不代表对 M3i 新 MSIX 的原生重放；显式 StartService 也不代表 SCM 自动恢复。首次夹具因错误的宿主路径检查在注入前退出，单独记录为失败。准确摘要和边界见[服务宿主恢复说明](../design/2026-09-08-service-host-recovery.md)。
- 证据提交 `9a863fd` 的[两项 CI 均成功](https://github.com/Water-Run/ClashSharp/actions/runs/34192795172)，下载四份 TRX 核对共 4513 项通过、0 失败、0 跳过。收据为 `ci-validation-m5f-docs.json`；该文档提交已同步到远端干净 main。

## M3h 启动组重置事务（2026-09-08）

- 启动组重置改为页面等待的异步事务：排空已有请求、取得独占许可，保留完整旧快照并在一个批次中提交四个启动默认值，再应用系统注册。失败时回滚完整快照并补偿注册；提交后清理失败保持同一决定并重试。
- 持久写入前支持取消，开始后等待应用和补偿结束；整组字段准备完成后才通知页面，其他组的重启提示基线保持。只调用启动注册，不调用网络、采样、语言及主题参与者。
- 新增 15 项回归及全部 2411 项主程序测试通过，零失败、零跳过。Release x64 完整构建零警告、零错误，完整 format 检查 1406 个文件、0 处变更。收据为 `local-validation-m3h.json` 和 `1.0.0-m3h-main.trx`。
- 三个持久阶段的重放使用中断注入及新服务实例，系统注册为模拟。实现和范围见[启动组保留事务](../design/2026-09-08-startup-group-reset.md)；网络组事务、完整设置代际迁移和实际页面验收继续推进。
- `97f5560` 的[两项 CI 均成功](https://github.com/Water-Run/ClashSharp/actions/runs/34189658318)，四份实际 TRX 共 4476 项通过、0 失败、0 跳过，包含上述 15 项回归。Core 行覆盖率 93.53%、分支覆盖率 86.73%；收据为 `ci-validation-m3h.json` 和 `ci-startup-reset-regressions-m3h.json`。

## M3g 开机启动请求顺序（2026-09-08）

- 修复开机启动开关在保存失败前已替换待应用状态的问题：现在先保存，再更新待应用状态和页面，正在执行的任务不会继续应用一次被拒绝的选择。
- 两项相反方向的并发回归先在旧逻辑上复现多一次应用调用，再通过修复。全部 2396 项主程序测试通过、0 失败、0 跳过；完整 Release x64 构建 0 警告、0 错误，format 检查 1404 个文件、0 处变更。
- 回归使用模拟系统注册回调，没有修改开发机开机启动设置。证据及实际系统验收范围见[开机启动请求顺序](../design/2026-09-08-startup-setting-request-order.md)及 `local-validation-m3g.json`。
- `c2e12d0` 的[两项 CI 均成功](https://github.com/Water-Run/ClashSharp/actions/runs/34188014553)，四份实际 TRX 共 4461 项通过、0 失败、0 跳过，包含上述两项回归。Core 行覆盖率 93.53%、分支覆盖率 86.73%；收据为 `ci-validation-m3g.json` 和 `ci-startup-request-regressions-m3g.json`。
- CI 开发归档 `10041357253` 共 317357219 字节，SHA-256 为 `c448ab6c9cae835ec84e55cf11d550b4e00a5009818d092de4823465169bdeab`；EXE 为 `0a0ae0c536329ccc6ad46f576287b8383def3bc020e462d22c2c0773e92d9067`，MSIX 为 `a61c9f0b99bb64fa06334312348bd785e17ae8a83548f13f1bd73e5d6377a8d0`。八个文件逐项匹配，远端无全局 .NET 的绿色校验通过，临时目录清理且无残留进程；没有正常安装、证书或代理修改。收据为 `installer-green-validation-m3g.json`。

## M3f 设置组批量重置（2026-09-08）

- 基础、通知、触发器、托盘、Windows 代理策略和地区设置改为通过存储端口一次重置整组，避免逐项保存途中遭遇独占拒绝而留下部分修改。规范键及旧键由现有注册表确定，仅允许移除服务拥有的键。
- 完整批次提交后才通知存储观察者，期间保持同一个普通写入许可；ViewModel 在成功后先更新整组字段再发通知，保存失败保留显示。基础组主题回调在完整状态发布后执行。
- 26 项新回归及全部 2394 项主程序测试通过、0 失败、0 跳过；Release x64 完整构建 0 警告、0 错误，完整 format 检查 1404 个文件、0 处变更。证据为 `local-validation-m3f.json`、`1.0.0-m3f-main.trx` 与同节点构建、格式日志。
- 生产存储继续使用 LocalSettings；需要外部运行时协调的启动和网络组仍通过既有流程，不由本接口写入。设计、旧键与验证范围见[设置组重置说明](../design/2026-09-08-preference-group-reset.md)。完整设置代际迁移及实际页面验收继续推进。
- `5e606c2` 的[两项 CI 全部成功](https://github.com/Water-Run/ClashSharp/actions/runs/34187062570)，四份实际 TRX 共 4459 项通过、0 失败、0 跳过，包含新增 26 项。Core 行覆盖率 93.53%、分支覆盖率 86.73%；收据为 `ci-validation-m3f.json` 和 `ci-preference-reset-regressions-m3f.json`。
- CI 开发归档 `10041032290` 共 317355420 字节，SHA-256 为 `749dae017ff57f06e9906993216b27af39f0d2670107b8e70af05e47e9e0014c`；EXE 为 `5ea0512d362cbc5af07a0c176bed56d589854ecfbe2393800bdf696c551fd4b9`，MSIX 为 `734771d9c28dec367262d60fb5704e036bb995a7fb2d48480d1817269f390541`。八个文件逐项匹配，远程仓库同步且干净；该 EXE 通过无全局 .NET 的绿色载荷校验及临时目录清理，没有正常安装、证书或代理修改。收据为 `installer-green-validation-m3f.json`。

## M5e 真实核心重载修复（2026-09-08）

- 在断网 Windows Sandbox 中，已安装的 `c0495f4` 服务成功启动真实 TUN 核心，配置及代理组查询通过，TCP 控制端口和混合代理端口保持关闭；重载代际 2 时出现暂存失败，代际 1 保留，随后停止成功。
- 原生描述符确认 Mihomo 缓存由内置 Administrators 持有，DACL 仍只授予 SYSTEM 和 Administrators 完全控制；旧策略只接受 SYSTEM 所有者。现提取只读运行权限策略，根仍必须由 SYSTEM 持有，受保护后代接受这两个受信所有者，继续拒绝外来权限及不完整的 SYSTEM 当前对象权限。
- 11 项新回归与本地主程序全部 2368 项通过，0 失败、0 跳过；完整构建 0 警告、0 错误，format 加载 1402 个文件、0 处变更。`f1fca8e` 的[构建测试和离线打包 CI 全部成功](https://github.com/Water-Run/ClashSharp/actions/runs/34184314054)，下载四份 TRX 核对共 4433 项通过、0 失败、0 跳过。Core 行覆盖率 93.53%、分支覆盖率 86.73%；见 `ci-validation-m5e.json` 和 `ci-runtime-security-regressions-m5e.json`。
- 同一提交的 CI MSIX 配合生产引擎完成真实启动、Rule→Global 重载、旧代际拒绝、停止与卸载；重载前实际缓存仍由 Administrators 持有。运行 `f12fac3fab7c4bb5ad43cf6c1c0648e8` 的八步 IPC 均符合预期，产品卸载完成后、兜底清理前已确认包、服务、证书与运行数据缺席；九项客体清理通过。收据为 `installer-service-runtime-m5e.json`。
- 同一候选再完成四次真实核心退出：三次自动恢复保留不可变代际及有效 Rule 配置，第四次进入重试耗尽状态且不再声明就绪；新的显式 Start 成功应用代际 2。核心保持运行时，生产卸载确认其退出并清理全部产品状态。运行 `4d7bc20fe93f4512b599d5fd82ce1cf9` 九项客体清理通过，收据为 `installer-service-recovery-m5e.json`。这些探针不操作 WPF，也不替代普通用户/UAC、服务宿主崩溃或完整发布矩阵。
- 两项成功原生运行没有服务宿主崩溃事件，Sandbox 已销毁、输入和主机代理未变；失败夹具结果单独保留。准确候选、失败路径及验证范围见[运行缓存所有者说明](../design/2026-09-08-service-runtime-cache-ownership.md)。
- CI 开发归档 `10040121447` 共 317355857 字节，SHA-256 为 `288cf60956c3c9d2363c2e99a62d355ea876fc9a5d89dd0ec9a5ca06de2dfc42`；EXE 为 `88aa4c15b5f733399fab14fb936017db1c58026f55786a4446d2d946e4a34005`，MSIX 为 `7d57b41bb13882eb3ab356f17ad7727675501d24590a1085d62547673c40c411`。八个文件逐项匹配，远程仓库同步且干净；该 EXE 在无全局 .NET 的远端完成绿色载荷自检与自有临时目录清理，没有正常安装、证书或代理修改。收据为 `installer-green-validation-m5e.json`。

## M3e 自定义主题色提交（2026-09-08）

- 颜色选择器此前先保存 Custom 模式，再保存颜色值；中途被独占操作阻止或保存失败时，可能只保留新模式。改为通过存储端口，在现有 settings authority 的同一个许可和可回滚批次内提交模式及规范化颜色。
- ViewModel 在提交成功后先更新两个字段，再发出属性通知；观察者看到完整选择，重启提示只刷新一次。保存失败保留原显示值，相同选择不重复通知。界面只负责取得颜色，不再分别编排两个设置写入。
- 颜色和托盘菜单对话框返回后再次检查页面取消令牌，页面离开后不接受迟到的保存结果。该检查沿用现有页面任务和对话框生命周期。
- 新增 9 项回归，覆盖规范化、完整通知时的独占等待、非法输入、独占拒绝、保存失败和重复选择。完整 Release x64 构建 0 警告、0 错误；本地主程序 2357 项通过、0 失败、0 跳过，完整 format 加载 1400 个文件、0 处变更。证据为 `1.0.0-m3e-main.trx` 及 `artifacts/verification` 下的 M3e 构建和格式日志。
- `b773f39` 的[构建测试和离线打包 CI 全部成功](https://github.com/Water-Run/ClashSharp/actions/runs/34182717547)。实际下载四份 TRX，共 4422 项通过、0 失败、0 跳过，包含新增 9 项回归；Core 行覆盖率 93.53%、分支覆盖率 86.73%。对应 `ci-validation-m3e.json`、`ci-accent-regressions-m3e.json`。
- 远程仓库同步同一提交且干净。CI 归档 `10039611720` 共 317359911 字节，SHA-256 为 `2e48b1bf37fe828fe40c5ef2500e2f6f6bad8c722394dfbc21b4770c32e7675d`；EXE 为 `603ee91d73ebcff31b04aa30bbd6cc3502126bf7653893d2f8510b90b913ee46`，MSIX 为 `da5da7f27bab6eaeed45df927b7815ee484c3f2474fe7dee7d1a672df5caf145`。8 个文件和声明逐项匹配，版本为 1.0.0.0。
- 该开发 EXE 再次通过远程无全局 .NET 的只读绿色校验：自包含运行时、5 个 WPF 原生库、4 个外部文件和 7 个 machine 文件通过，临时目录及本次进程已清理。没有执行正常安装或修改证书、代理；收据为 `artifacts/verification/installer-green-validation-m3e.json`。
- 这次变更继续使用既有 LocalSettings authority；没有将其声明为完整文件代际切换，也没有代替真实 WinUI 颜色选择器验收。

## M3d 设置地址编辑与批量保存（2026-09-08）

- 修复无效连接测试地址被静默丢弃的问题：关闭对话框前校验，在原窗口显示错误并聚焦错误字段，保留草稿；增加六种语言的提示和输入框无障碍名称。
- 统一恢复默认的行为：先填入草稿，保存才提交，取消保持原值。页面取消后不接受迟到的保存结果。
- 三个地址改为通过一个存储端口，在现有 authority 的同一个写入许可下提交，避免独占重置在字段间插入；通知只在全部字段提交后发出，ViewModel 成功后才发布显示值。
- 新增 15 项回归；完整 Release x64 构建 0 警告、0 错误，主程序 2348 项测试全部通过、0 跳过，format 加载 1400 个文件、0 处变更。设计和范围见[连接测试地址的编辑与批量保存](../design/2026-09-08-connection-test-settings-editor.md)。这不关闭完整设置代际迁移或真实 UI 验收项。
- `027d1c7` 的[构建测试与离线打包 CI 均成功](https://github.com/Water-Run/ClashSharp/actions/runs/34180602028)。下载核对四份 TRX：Main 2348、Installer Core 970、Presentation 125、Windows 970，共 4413 项通过、0 失败、0 跳过，新增 15 项回归全部实际执行。Core 行覆盖率 93.53%、分支覆盖率 86.73%；收据为 `artifacts/verification/ci-validation-m3d.json`。
- 同次 CI 开发归档 `10038930851` 共 317356241 字节，SHA-256 为 `5309e577720691bf9f20a788ba5638f313b21e898f7783673dfb374b6761c484`；EXE 为 `42cb40274a4d38853ae0ed3636858fc3ccf28d9a0b8fb2eabb54291b876272dc`，MSIX 为 `6d5782edd8b4d6a2d0025f295bd2989a265d092436991de1f7f050103edacf3f`。远程仓库同步同一提交且干净，归档及 8 个文件逐项匹配，第三方声明与包内内容一致。
- 该 EXE 在远程无全局 .NET 环境通过 `--verify-payload`，确认自包含运行时、5 个 WPF 原生库、4 个外部文件和 7 个 machine 文件；临时目录已清理且没有本次残留进程。没有启动正常安装或改变证书、代理。收据为 `artifacts/verification/installer-green-validation-m3d.json`；这不是 WPF 页面操作验证。
- 清除 main 分支保护中遗留的 `Rust Installer` 和 `Rust SandboxTest` 检查，将必需检查对齐为 `.NET build, format, and test` 与 `Offline Installer package`，均绑定 GitHub Actions app 15368。保留 strict 和其他保护配置；API 回读收据为 `artifacts/verification/branch-protection-m3d.json`。

## M5d 服务就绪修复（2026-09-08）

- Verified 中断后的新 parent 复核发现服务停止；客体 .NET Runtime 1026 事件确认内部构造函数无法被 DI 类型注册解析。失败客体已销毁、输入和主机代理未变，失败运行不计作通过。
- 使用显式工厂装配服务，初始化与命令、关闭共享串行边界；构造和解析容器不再访问运行目录。安装器复用 Core IPC 合同，在发送凭据前校验 SCM 管道进程，Hello 后复核版本、就绪状态和进程身份。
- 补齐精确关联的私有运行目录卸载；完整结构预检后才删除，失败时保留关联与事务。设计与证据边界见[服务装配、就绪确认与运行目录清理](../design/2026-09-08-service-readiness-and-runtime-cleanup.md)。
- 本地完整 Release x64 构建 0 警告、0 错误；format 检查 1400 个文件、0 处变更。Main 2333、Installer Core 970、Presentation 125、Windows 963 项通过，共 4391 项，0 失败、0 跳过；6 项真实用户证书存储用例留给 CI/客体执行，未在开发机运行。
- 后续收紧运行目录清理的硬链接检查；临时文件回归确认目录外别名的内容和只读属性完整保留，5 项清理测试通过。开发机仍未执行真实安装或证书存储测试。
- `c0495f4` 的[两项 CI 全部成功](https://github.com/Water-Run/ClashSharp/actions/runs/34176167742)；实际下载的四份 TRX 共 4398 项通过、0 失败、0 跳过，包含 CI 中的真实证书存储测试。Core 行覆盖率 93.53%、分支覆盖率 86.73%。
- 使用同次 CI MSIX 与包含新握手的签名生产引擎，在客体完成正常安装、修复、卸载，以及七个持久阶段 parent 终止和一个服务文件占用场景。八项故障恢复均由新 parent 完成，产品卸载快照先于兜底清理；未观察到服务崩溃事件，九项客体清理通过，Sandbox 已销毁、输入和主机代理未变。
- 文件占用时返回 Uncertain 并保留 MachineRemovalAuthorized 日志；释放句柄后重新卸载成功，没有把部分完成误报为完整回滚。具体阶段、候选摘要与证据边界见上述设计记录及 `artifacts/verification/installer-engine-m5d.json`。
- 同一签名候选完成安装 Verified 中断后的实际客体 OS 重启：重启后日志摘要一致，延迟服务自动启动，新 parent 完成握手和安装恢复，再卸载并确认产品资源移除。含续跑标记的十项客体清理通过，Sandbox 已销毁、输入和主机代理未变；收据为 `artifacts/verification/installer-engine-reboot-m5d.json`。正常 OS 重启不代表硬断电矩阵通过。
- 同一 `c0495f4` 候选补充服务待删除场景：独立原生只读句柄跨卸载保持，服务停止时机器载荷、包和两类证书保留，返回 Uncertain 并保留阶段 2；释放句柄后，新 parent 完成卸载。九项客体清理通过且无服务崩溃事件，Sandbox 已销毁、输入和主机代理未变。收据为 `artifacts/verification/installer-engine-delete-pending-m5d.json`；未声称实际捕获 Win32 1072 返回码，也未改用 M3d 的新 MSIX。
- 远程仓库已同步 `c0495f4` 且工作树干净；同次 CI 开发安装器在无全局 .NET 的环境通过只读绿色入口，临时目录和进程已清理，正常安装及证书/代理变更均未执行。
- WPF 交互和完整发布矩阵仍待完成。下列 M5c 的完成结果限于事务返回与瞬时资源观察，不能作为服务持续可用的证明。

## M5c 验证记录（2026-09-08）

- 生产 parent/helper 在独立 Windows 11 客体中完成安装、修复和卸载；4 个 parent 与 3 个 helper 进程全部正常退出，三个事务均达到 Verified、返回 `installer.completed`，没有待恢复日志。
- 安装与修复后精确包已注册、服务运行、机器及用户物理证书存在；产品卸载后的快照确认包、服务、证书、服务目录与状态文件已移除。快照先于测试兜底清理，客体 9 项清理通过，Sandbox 已销毁、只读输入与本轮主机代理未变。
- 正式打包显式选择生产运行时，开发打包显式关闭安装入口，普通项目构建仍默认预览。签名、固定签名者、嵌入清单及时间戳门禁保持；真实 MSBuild 检查 3 种配置与 3 种非法启用组合，CI 已接入该检查。
- 本地完整 Release x64 构建 0 警告、0 错误；Presentation 125 项、打包与仓库结构 27 项通过，0 失败、0 跳过。完整 format 加载 1394 个文件、0 处变更；18 个 PowerShell 源文件在 5.1 与 7 均解析通过。
- WPF 候选只确认客体内签名与启动；computer-use 原生管道不可用，未完成页面操作。首轮快照的 CIM 拒绝问题通过改用服务控制器和注册表查询解决，没有将失败运行计作通过。
- 本轮只使用一次性客体测试信任及 M5b 既有的卷根 ACL 夹具；完整升级、跨账户、重启与故障矩阵仍是发布验收要求。详细证据和边界见[安装入口配置与真实事务验证](../design/2026-09-08-installer-runtime-activation.md)。

## M2 验证记录

- 新增可取消、可等待完成的页面操作队列；连续接受的修改按顺序执行，卸载取消当前及排队操作，再次进入等待旧操作收尾。订阅、配置、节点选择和连接关闭接入该边界。
- 连接刷新以修订号防止较早的 HTTP 返回或失败覆盖较新的刷新、实时流或不可用状态。
- 功能磁贴使用原生 Button 的键盘、焦点和 UIA Invoke；绑定命令可用性与异步执行状态，保留拖动误触抑制。纯信息磁贴保留可读内容。
- 语义版本固定 `1.0.0`，程序集和 MSIX 使用 `1.0.0.0`；关于页与版本磁贴读取统一版本入口。
- `dotnet build ClashSharp/ClashSharp.slnx -c Release -p:Platform=x64 --no-restore`：18 个项目，0 警告、0 错误。
- 主程序 2255、Installer Core 548、Presentation 89、Windows 安全子集 348：共 3240 项通过，执行范围内没有跳过项。本机仍不执行修改当前用户 TrustedPeople 的 3 项测试；完整 Windows 组由隔离 CI 执行。
- `Platform=x64` 的完整 solution format 验证通过，加载 1246 个源文件、0 处变更，无工作区加载警告；TRX 为各测试项目的 `TestResults/1.0.0-m2-*.trx`，format 日志为 `artifacts/verification/format-1.0.0-m2.log`，这些本地证据不进入发布资产。

## M3a 验证记录（2026-09-07）

- 主控功能磁贴改为可等待的异步命令，执行状态持续到窗口和内部任务收尾；连续点击同一命令不会重复执行。页面卸载解除交互回调并取消、等待已接受的操作。
- 主控磁贴与设置页复用备份/恢复演示器，实际执行导入/导出文件选择和事务，不再只导航到设置页。选择文件后和确认后再次检查取消，避免已离开的页面应用迟到结果；导入成功立即刷新模式、布局和运行摘要。
- 测速窗口持有测速任务，关闭时取消并等待任务结束后释放资源；结果留在窗口供阅读，错误通过应用错误边界观察。
- 设置页的对话框、重置、清理、导入/导出、网络诊断接入显式操作所有权。页面再次进入前等待旧操作结束；诊断窗口关闭时同样取消并等待内部诊断任务。原有服务侧的提交和补偿事务保持其职责。
- Release x64 完整构建 0 警告、0 错误；主程序 2260 项测试通过、0 跳过；完整 format 加载 1250 个文件且 0 处变更、无加载警告。证据：`ClashSharp/ClashSharp.Tests/TestResults/1.0.0-m3a-main.trx`、`artifacts/verification/format-1.0.0-m3a.log`。
- 远端隔离 SDK 已成功还原锁定依赖，并完整构建 `6eccb93`，0 警告、0 错误。

## M3b 验证记录（2026-09-07）

- 连接页支持对已显示的进程、目标、规则和代理名称进行本地搜索，实时快照继续保留筛选条件。分别展示尚未刷新、加载中、没有活动连接、没有匹配结果和核心不可用状态，显示筛选数量，关闭全部始终针对完整连接集合。
- 连接行改为目标、进程/代理、规则的分行布局，上传/下载与关闭动作独立放置；补齐关闭工具提示和流量无障碍说明，新增文案覆盖全部六种语言。
- 所有应用日志生产者在 SQLite 入库前经过同一有界处理：去除常见凭据字段、完整 URI、用户目录名称和方向控制字符，限制单字段大小。该处理不能代替调用方避免记录私有配置的责任。
- Release x64 完整构建 0 警告、0 错误；主程序 2279 项测试通过、0 跳过；完整 format 加载 1252 个文件且 0 处变更、无加载警告。证据：`ClashSharp/ClashSharp.Tests/TestResults/1.0.0-m3b-main.trx`、`artifacts/verification/format-1.0.0-m3b.log`。

## M3c 验证记录（2026-09-07）

- 读取会话跟踪全部已接受的任务；替换读取只发出取消，不会提前丢失仍在收尾的任务。新增 DrainAsync、外层取消令牌和取消回调异常的观察，异常路径也释放当前读取的资源。
- 日志页离开时取消并等待查询、实时流和清理队列；重新进入先等待旧访问结束。清理预览由对话框独立持有，关闭时解除事件、取消并等待全部预览任务。补上首次预览，避免打开窗口后一直停留在占位状态。
- 日志清理通过独立操作队列执行，重复点击被阻止；提交期间禁用页面并等待旧查询退出，搜索与筛选不能替换清理任务的取消源。现有 SQLite 已提交修改的恢复语义不变。
- 5 项新增生命周期用例覆盖旧查询迟迟不退出、外层取消以及取消回调失败；定向 26 项、完整主程序 2284 项通过，均 0 跳过。Release x64 完整构建 0 警告、0 错误；完整 format 加载 1252 个文件，0 处变更、无加载警告。证据为 `1.0.0-m3c-main.trx` 与 `artifacts/verification/build-1.0.0-m3c.log`、`format-1.0.0-m3c.log`。UI 快速切页仍待交互式桌面验收。

## M4a 验证记录（2026-09-07）

- 将四项 GeoData 固定到上游数据提交和 SHA-256，提供有界下载与缓存校验脚本；已从提交地址重新下载全部输入，四项摘要与原验证副本一致。损坏缓存被拒绝，原文件保留且未启动后续下载。
- 固定 SDK 和微软 x64 framework MSIX 的版本、长度、摘要、签名者；最终载荷 provenance 记录这些输入。第三方数据的完整来源与许可归档仍是正式候选工作，固定字节本身不代表已完成发行归档。
- 实际远端打包修复了主程序缺少自包含 .NET、辅助 apphost 重复、启动任务文件名占位符、AppxPackageDir 缺少尾部分隔符，以及 SDK 多架构依赖布局与产品 x64 载荷不一致的问题。主 MSIX 包含 .NET 10.0.5；WinUI 使用随包携带并验证的 Windows App Runtime framework。
- 新增最终 MSIX runtimeconfig、RID、必要文件与 PE 架构验证，1 个完整 ZIP fixture 通过、19 个缺失/损坏/错误架构 fixture 被拒绝。CI 增加实际离线开发安装器打包任务。
- 远端完整开发打包成功，构建输出 0 编译警告、0 错误；开发 Installer 为自包含单文件，发布目录含 7 个文件、323681257 字节，主 MSIX 为 191931042 字节、版本 1.0.0.0，包含 1 个经验证的 x64 framework 依赖。
- 开发 EXE SHA-256：`d92806fd0ee50464c6b732533684af1ab583055f4ddcdc6c34cdaccdc33a8845`；主 MSIX SHA-256：`26ea12346d7cad949ba598c1d29e8c253dde1e46f1d1a6cb9b1574d1b2927c99`。开发证书仅用于远端构建，没有加入信任存储，没有执行安装或启动主程序。
- 同一开发包归档为 `artifacts/validation/ClashSharp-1.0.0-development.zip`，315572821 字节，传回本机后核对 SHA-256 为 `c97aef0bc64257c47b83562a1e69032d9735b7bd633e065ffd2980b63e9afcd2`。直接读取同次构建程序集中的嵌入清单，以生产 C# codec 验证通过，再逐一核对归档内 4 个 payload 文件的长度和摘要；清单含 7 个 machine 文件合同。该验证未加载运行安装器或主程序。
- 本机完整 Release x64 构建 0 警告、0 错误；主程序 2279 项测试通过、0 跳过；PowerShell 5.1/7 解析全部 12 个源文件；完整 format 加载 1252 个文件，0 处变更、无加载警告。证据为 `1.0.0-m4a-main.trx`、`artifacts/verification/format-1.0.0-m4a.log` 和远端开发打包日志。
- 开发包保留明确的 Development-Unsigned 标识，生产 mutation 编译门仍关闭；远端尚无已登录桌面且系统为 Server 2025，不能将本次打包通过当作 Windows 11 实际安装或正式 1.0.0 发布。构建说明见 [Installer README](../../ClashSharp/Installer/README.md)。

## M4b 验证记录（2026-09-07）

- 修复正式产物名与载荷验证、签名验证、提权启动的约定不一致：三个适配器统一读取 Core 中的 ClashSharp-Installer.exe 常量。拒绝内部构建文件名、Development-Unsigned 名称，以及相对路径、UNC 和设备路径；构建脚本核对实际 EXE 返回的正式名称，避免两个语言的约定再次漂移。
- 新增 --verify-payload 只读进程入口，与 UI、提权分支互斥；保留参数拼写、重复参数和混合模式错误直接返回，不会回退到 UI。Windows 审计适配器复用生产文件锁、精确集合、摘要、MSIX 身份和包内 machine 文件验证，返回不可变证据前关闭所有句柄。
- 打包流程启动最终自包含 EXE，以有界等待读取 JSON，再与生成清单核对版本、名称、摘要、数量和总字节数。子进程及两个输出读取任务均由构建持有；失败时现有产物清理逻辑不会保留无效 release 目录。
- 远端实际开发打包与最终 EXE 只读校验通过：4 个外部载荷文件、7 个包内 machine 文件、版本 1.0.0.0；未创建安装 UI、未提权、未修改服务或证书信任。日志为 artifacts/verification/development-package-1.0.0-m4b.log。这项检查不代表正式签名信任或安装矩阵通过。
- Release x64 完整构建 0 警告、0 错误；主程序 2284、Installer Core 548、Presentation 97、Windows 安全子集 365，共 3294 项通过，执行范围内 0 跳过。新增用例涵盖 9 项正式入口名称/路径拒绝、8 项只读分支路由和 8 项载荷审计行为与本地路径边界。当前用户 TrustedPeople 的 3 项真实修改测试仍仅在隔离 CI 执行。
- 完整 format 加载 1256 个源文件、0 处变更、无工作区警告；PowerShell 5.1 与 7 均解析 12 个源文件通过。本地证据为各测试项目的 1.0.0-m4b-*.trx 及 artifacts/verification 下的 build、format、test 日志。

## M4c 验证记录（2026-09-07）

- 发现原有文件存储的锁只在对象内生效，不能阻止两个提权助手同时读取同一代日志并修改机器。默认 helper composition 现在在创建存储、读取日志前取得机器范围的独占租约，持有到所有会话资源关闭后。
- 固定 Global 互斥对象独立于目标账户，实际权限仅允许 SYSTEM 与 Administrators；已有对象必须在打开的句柄上验证。专用线程保持 Windows mutex 的线程所有权，异步释放会等待该线程完成资源清理；取消、初始化失败和清理异常不会提前放开仍在运行的事务。
- 26 项定向测试通过，覆盖两个实例互斥、取消前后语义、打开和释放异常、重复释放、abandoned 对象接管，以及 helper 认证、存储创建和锁释放的顺序。本机只创建随机 Local 测试对象，没有打开固定生产互斥对象。
- 远端以两个独立进程调用编译后的默认实现，确认第二进程被拒绝、正常退出后可再次获取，以及终止持锁进程后可恢复获取；默认对象实际权限验证通过。脱敏收据为 artifacts/verification/authority-validation-m4c.json，验证程序集 SHA-256 为 dc71cb8a62ec797cf3b824edfc1fbfd19f2f669a2647b4f7628e4a47a59c7a47。测试未调用安装、服务、证书信任或网络接管。
- 完整 Release x64 构建 18 个项目，0 警告、0 错误；主程序 2284、Installer Core 548、Presentation 97、Windows 安全子集 385，共 3314 项通过，执行范围内 0 跳过。完整 format 加载 1260 个源文件，0 处变更、无工作区警告。证据为 1.0.0-m4c-*.trx 及 artifacts/verification 下的 build、format、test 日志。
- 设计与验证范围见 [Installer 跨进程执行边界](../design/2026-09-07-installer-authority-exclusion.md)。双账户持久阶段和正式运行开关仍未完成，不以本次互斥测试代替生产安装矩阵。

## M4d 验证记录（2026-09-07）

- 修复 C# Installer 写入 v2 日志、主程序只检查旧版标记的集成错误。固定目录与文件名改由同一份声明提供；主程序仅链接常量源文件，不依赖 Installer 运行程序集。
- 启动时检查旧版标记与当前日志；空文件、损坏内容和尚未清理的 Verified 日志均阻止启动，异常路径按 Invalid 关闭准入。读取不创建、解析、修复或清除日志，也不阻碍 authority 的原子替换。
- 新增真实日志存储到主程序启动 gate 的跨组件测试：安装、修复、卸载的每个持久阶段都阻断新的启动，只有 ClearVerifiedAsync 完成后开放。新旧路径的文件冲突、独占打开、符号链接、空目录与相对根目录另有验证。
- 51 项定向测试通过；完整 Release x64 构建 18 个项目，0 警告、0 错误。主程序 2305、Installer Core 548、Presentation 97、Windows 安全子集 385，共 3335 项通过，执行范围内 0 跳过。当前用户 TrustedPeople 的 3 项真实修改测试继续仅在隔离 CI 执行。
- 完整 format 加载 1263 个文件，0 处变更、无工作区警告；证据为 1.0.0-m4d-*.trx 及 artifacts/verification 下的 build、format、test 日志。上一节点 6049d9a 的构建/测试与离线开发打包 CI 均已成功。
- [启动日志设计说明](../design/2026-09-07-installer-startup-journal.md)记录判定和验证范围。新 Installer 与 App 生命周期锁的衔接、跨账户换绑及正式安装矩阵仍在推进；不能把单次日志观察当作全部并发安装保护。

## M4e 验证记录（2026-09-07）

- Parent 创建执行会话前取得 App 生命周期锁，helper 在机器范围互斥之后、创建事务存储之前取得自己的句柄，均持有至会话资源结束。运行中的 App 会在 Prepared 和机器修改前阻止安装；parent 退出或取消不会提前释放 helper 的锁。
- Parent 仅在已验证 SID、已知目录和已锁定的普通祖先路径中创建协调文件；helper 只读打开已有文件，不创建、重定向、修复或写入用户目录。固定文件名与 App/watchdog 共用声明。
- MSIX 为共享产品目录增加精确的虚拟化排除与所需 capability，避免包内与包外进程使用不同协调文件。没有禁用整个 AppData 或注册表虚拟化；这项清单变更的实际包内桌面行为仍待安装验收。
- 42 项定向测试通过；完整 Release x64 构建 18 个项目，0 警告、0 错误。主程序 2306、Installer Core 548、Presentation 97、Windows 安全子集 407，共 3358 项通过，执行范围内 0 跳过。3 项 TrustedPeople 实际修改测试继续仅由隔离 CI 执行。
- 完整 format 加载 1266 个源文件，0 处变更、无工作区警告。构建、格式与完整回归证据为 artifacts/verification 中的 1.0.0-m4e 日志及各测试项目的同名 TRX。
- 远端独立进程验证了生产 App 与 Installer 文件锁实现之间的互斥、终止 parent 后 helper 保持阻断、最后持有者退出后重新获取，以及真实目录 junction 在文件创建前被拒绝。验证替换了目标 profile，未执行完整安装或包内 GUI；所有自有进程已结束。收据为 artifacts/verification/application-lock-validation-m4e.json，Windows 程序集 SHA-256 为 fa5a0ff2675137d79766ca2647b01ecd6100142f421ca29d20ef5e42308a4384，watchdog 程序集为 e4196617a1923d78aa237b81cd1a3ca9cdc41d6652f0fddd1c6d6244a4323b66。
- 上一节点 71d1998 的构建/测试与离线开发打包 CI 均已成功。本节点设计及证据边界见 [Installer 与 App 生命周期协作](../design/2026-09-07-installer-app-lifetime.md)；仍继续跨账户持久阶段、正式候选安装与桌面验收。

## M4f 验证记录（2026-09-07）

- App 在安装器持有协调锁时立即结束新的启动，不再等待十秒后留下诊断窗口。OwnershipUnavailable 状态通过既有 startup gate 终态关闭准入，随后无窗口退出，不读取事务日志、不启用 watchdog 或网络接管，避免包内诊断进程继续阻挡 MSIX 替换。
- 新增专用诊断状态，并扩展关闭窗口/主机退出验证：Pending、Invalid 和 OwnershipUnavailable 三种阻断状态均不会调用网络关闭协调器。主程序 2309 项测试通过、0 跳过；完整 Release x64 构建 18 个项目，0 警告、0 错误。Installer Core、Presentation 和 Windows 适配器未改变，其最近完整验证见 M4e。
- 完整 format 加载 1266 个源文件，0 处变更、无工作区警告。证据为 1.0.0-m4f-main.trx 以及 artifacts/verification 下的同节点 build、test、format 日志。
- 前一节点 8188b45 的两项 CI 和远端实际开发打包已成功；最终 MSIX 中只存在预期的共享目录排除，并包含所需 capability。打包结束的真实 EXE 只读载荷审计通过，4 个外部文件、7 个 machine 条目、版本 1.0.0.0。
- 该开发包归档已传回本机，artifacts/validation/ClashSharp-1.0.0-development-8188b45.zip 为 315610235 字节，两端 SHA-256 一致：5f78af9b32057899f18c31901117e1c5b14b54ff15f02ce1a0760fe6aa2bf995。EXE 摘要为 a6e8fc800580ed4a289dc2baf4cceab65f64ba86aed2d53c2d1298f9b55fbd6e，主 MSIX 为 ea3b8f01000a730419c05167d335c694919cda455d24889af2a9569a667fca2e；收据为 artifacts/verification/development-package-8188b45-receipt.json。它来自 8188b45，不包含本节点后续的 App 退出变更，且仍是 Development-Unsigned 开发资产。

## M4g 验证记录（2026-09-07）

- 修复 WPF 品牌图标四处交叉镂空和长阴影未正确叠加的问题：每个 SVG 路径对应独立绘制层。ICO 直接读取实际 WPF 资源，统一既有安装器紫色配色，补齐为 16 / 20 / 24 / 32 / 40 / 48 / 64 / 128 / 256 九帧；16–32 px 有显式横笔与 C 光学调整。
- 新增无需外部渲染器的 Windows PowerShell 5.1 离屏生成脚本与 CI 字节验证。本机与远端独立生成的 381038 字节完全一致，SHA-256 为 c86245d4dcfa50b9ffde36b85f7173fbe5b30d5257a6cbc65a833bca643719bf；Windows 解码九帧的像素与生成结果逐字节一致。旧版四处镂空均复现，新版均为实心白色。
- Release x64 完整构建 0 警告、0 错误；Presentation 97 项与仓库约定 16 项通过，均 0 跳过；PowerShell 5.1/7 解析全部 13 个维护脚本通过。证据为 1.0.0-m4g-presentation.trx、1.0.0-m4g-topology.trx、artifacts/verification 中的 build/test 日志及 installer-icon-validation-m4g.json。
- 完整 format 加载 1266 个源文件，0 处变更、无工作区警告。上一节点 77b4f91 的构建/测试和离线开发打包 CI 均已成功。本节点未执行桌面 DPI 或 UAC 检查，详见 [图标生成说明](../design/2026-09-07-installer-icon-generation.md)。

## M4h 验证记录（2026-09-07）

- 加固跨账户协议的前置边界：普通 ownership policy 不再凭 AllowReassociation 标记给未知或外来归属生成新 token；普通 association store 不再凭该标记覆盖旧 owner/token 或损坏证据。同 owner 继续复用原凭据，干净安装继续生成新凭据。
- Core 新增 12 个用例；Windows 适配器将原有覆盖用例替换为按条件独立验证的拒绝矩阵，新增 8 个执行用例。所有拒绝路径核对没有停止服务或改写 association，并保留旧字节、服务及载荷状态。
- 完整 Release x64 构建 0 警告、0 错误；Installer Core 560 项、Windows 开发机安全子集 415 项通过，均 0 跳过。当前用户 TrustedPeople 的 3 项修改测试继续仅由隔离 CI 执行。证据为 1.0.0-m4h-core.trx、1.0.0-m4h-windows-safe.trx 和 artifacts/verification 下的同节点日志。
- 完整 format 加载 1266 个源文件，0 处变更、无工作区警告；上一节点 b55da9d 的两项 CI 均已成功。真正换绑仍需双 owner 持久状态、证书账本保存、目录权限转移、旧账户后续卸载和确认界面；本节点没有开启该功能。接入边界见 [跨账户换绑说明](../design/2026-09-07-installer-owner-transfer.md)。

## M4i 验证记录（2026-09-07）

- 新增纯 Core 的私有换绑状态协议，绑定旧/新 owner、不同凭据、可信 profile 记录、两份独立证书账本及准确的普通 Prepared continuation。八个阶段只允许相邻推进或精确重放；创建、保存和删除有独立的身份/摘要/阶段检查。
- 私有 codec 使用源生成元数据、16 KiB/深度 4 限制和规范字节比较。拒绝含糊 JSON、身份缺失、目录设备名及不能无损编码的 profile 字符；诊断不暴露凭据或路径，公开 v2 continuation 格式不变。
- 新增 161 项协议测试；完整 Installer Core 721 项通过、0 跳过，行覆盖率 93.29%、分支 85.98%。完整 Release x64 构建 18 个项目，0 警告、0 错误。证据为 artifacts/verification 下的 build-1.0.0-m4i.log、test-1.0.0-m4i-core.log 和 coverage-1.0.0-m4i 目录。
- 完整 format 加载 1275 个源文件，0 处变更、无工作区警告。本节点尚未新增实际持久存储或原生迁移执行器，也未启用跨账户 UI 或 production gate；下一步继续专有 ACL、原子写入和中断重放。设计见 [跨账户换绑说明](../design/2026-09-07-installer-owner-transfer.md)。前一节点 5fe0ab9 的两项 CI 已成功，并已同步到远端。

## M4j 验证记录（2026-09-07）

- 完成私有迁移记录的 Core store 与 Windows 文件系统适配器。Core 在写入/删除确认丢失或取消后读取实际状态，精确重放不重复写入，未知状态不自动重试或清除。字节缓冲及时清零，并拒绝同一 store 的并发调用。
- 私有记录固定在 InstallerAuthority/v1，目录与文件仅允许 SYSTEM/Administrators 的准确 ACL；保留共用产品根目录原权限。原生句柄验证普通对象、单硬链接及权限，目录链持续固定；临时文件创建时即设定私有 ACL，flush 后原子替换固定叶子。
- 新增 Core 22 项、Windows 21 项用例。完整 Core 743 项、Windows 安全子集 436 项通过，执行范围内 0 跳过；Core 行覆盖率 93.39%、分支 86.17%。完整 Release x64 构建 18 个项目，0 警告、0 错误；format 检查 1284 个源文件、0 处变更、无工作区警告。
- 远端隔离 ProgramData 原生验证 74 项断言通过，覆盖跨进程重启、八阶段读写/清除、精确 ACL、目录句柄及硬链接/符号链接/超大文件拒绝。生产机器互斥锁覆盖每次验证进程；未修改代理、SCM、包或证书。收据为 artifacts/verification/private-persistence-validation-m4j.json，Core 摘要 e4068c57e4e0a47b30c4137591dc4e303f10dcba4042caceb9a9d328442425ce，Windows 摘要 f0c85a4c9ec34d192503c4afc7ae887c5a644cdec0149cee68236728eb56f88d。
- 测试、覆盖率和格式日志位于 artifacts/verification 下的 1.0.0-m4j 文件，Windows TRX 位于对应项目 TestResults。前一节点 3715d1b 两项 CI 已成功。本节点尚未将存储接入 helper 或启用实际跨账户迁移，继续普通入口只读阻断、原生迁移、确认界面及正式候选验收；见 [跨账户换绑说明](../design/2026-09-07-installer-owner-transfer.md)。

## M4k 验证记录（2026-09-07）

- 普通 helper 在取得机器独占锁之后、申请 App 锁与建立普通事务资源之前，只读检查私有换绑记录。独立 presence 端口只验证句柄与固定叶子存在性，不读取凭据内容、解析 JSON 或创建缺失目录；所有阶段及损坏记录均阻断普通操作。
- 新增 14 项 Windows 测试，验证缺失链、未知权限、检查与 authority 的资源顺序及取消后排空。定向 63 项、完整 Windows 安全子集 450 项通过，均 0 跳过。全解决方案 Release x64 构建 18 个项目，0 警告、0 错误；format 检查 1285 个源文件，0 处变更、无工作区警告。
- 远端生产只读端口通过 31 项原生断言，覆盖全部八阶段、空/损坏/超大记录保留、宽松 ACL 拒绝及缺失目录零创建。收据为 artifacts/verification/owner-transfer-admission-validation-m4k.json，Windows 程序集 SHA-256 为 485e6763ef6c100c867a06a3c5f68b2ecae000c9aa598992a56727b3ec548fba。测试只触及隔离验证目录，机器服务、包、证书及代理保持。
- Core 和 Presentation 未修改，最近完整 Core 743 项、覆盖率证据见 M4j；本节点日志为 artifacts/verification 下的 build/test/format-1.0.0-m4k 文件及对应 Windows TestResults。M4j 两项 CI 已成功并已同步远端。本节点接入的是普通入口的拒绝边界，专用迁移操作、确认界面及签名候选验收继续推进。

## M4l 验证记录（2026-09-07）

- 新增 Core 持久阶段编排器：恢复先核对准确 snapshot，六个修改边界分别验证并保存进度；取消、错误和未知确认不会提前推进或清除记录。已是 Verified 的恢复仍必须再次检查最终后置条件和普通 Prepared 启动屏障，完成后才返回同一普通 continuation。
- 新增 31 项测试，结合生产 Core 私有 store、真实临时 v2 日志和模拟系统修改端口，验证全阶段恢复、每个步骤前后中断、无重复修改、取消期间排空、并发拒绝、最终验证失败、启动屏障丢失及最终确认失败后的再次验证。
- 完整 Installer Core 774 项通过、0 跳过，行覆盖率 93.44%、分支 86.23%。全解决方案 Release x64 构建 18 个项目，0 警告、0 错误；format 加载 1288 个源文件、0 处变更、无工作区警告。证据为 artifacts/verification 下的 build/test/format-1.0.0-m4l 文件以及 coverage-1.0.0-m4l 目录。
- Windows 阶段执行端口尚未实现，专用认证入口、证书归属保留、双账户清理和确认界面继续推进；本节点未进行真实系统修改或启用生产安装。前一节点 246f01d 已同步远端，[两项 CI 均通过](https://github.com/Water-Run/ClashSharp/actions/runs/34063818830)。

## M4m 验证记录（2026-09-07）

- 新增 Windows 旧服务删除步骤：先核对候选、准确普通 Prepared、旧 profile、机器 ACL 与 association，只按旧身份验证和删除 SCM 服务。原 association 和启动日志保留，已删除状态可重放，取消等待已拥有的步骤结束后才释放目录资源；尚未接入完整迁移入口。
- 真实远端验证发现初始 association 写入会因外层目录 DELETE 访问与内层拒绝共享删除发生错误 32，同时原 metadata-only 只读句柄允许目录改名。共用 lease 改为读取目录内容及安全信息、共享读写且不共享删除；移除两种模式参数，兼容嵌套保护并保持名称固定。详见[换绑设计的 M4m 记录](../design/2026-09-07-installer-owner-transfer.md)。
- 新增 21 项服务边界测试、3 项真实临时文件系统回归。完整 Windows 安全子集 474 项通过、0 跳过；18 项目 Release x64 构建 0 警告、0 错误，format 1291 文件、0 处变更、无工作区警告。本机没有实际 SCM、系统 ACL、证书或代理修改。
- 远端真实 SCM 步骤通过 12 项断言，确认删除、重复执行、陌生凭据拒绝、association/日志保留及自有服务清理。服务始终停止，候选是测试 manifest，未验证双 App 屏障或候选签名，不能计作完整安装验收。修复后同一程序集另通过四个独立进程的 105 项私有存储/准入回归。
- 证据为 artifacts/verification 下的 build/test/format-1.0.0-m4m 日志、service-removal-validation-m4m.json、private-state-regression-m4m.json 和原生句柄对照收据；Windows 程序集 SHA-256 为 deb128c8dd39fafeb04b748935d71b61e10434e1867a11cdd4d1db3b310be395。前一节点 3806050 已同步远端且两项 CI 通过，剩余 Windows 迁移步骤、账户清理及桌面验收继续推进。

## M4n 验证记录（2026-09-07）

- 修复生产服务准备运行目录时重写共享根权限的问题。服务只读验证 Installer 创建的准确 owner ACL，并固定完整目录链；仅准备自己的私有运行后代，不再创建或改写共享根，避免破坏用户读取 association 和安装器后续修复的契约。
- 原生只读目录句柄、权限策略和观察模型抽取为两端共用源码，各自编译、独立部署；保留安装器协议错误边界及原有模块依赖方向。
- 新增 21 项服务边界测试，完整主程序 2330 项、Windows 安全子集 474 项通过、0 跳过。完整 Release x64 构建 18 项目零警告、零错误；format 1299 文件、零处变更、无工作区警告。Core 和 Presentation 没有变更，前一节点 a8615b0 的两项 CI 已通过。
- 远端使用生产 Installer 创建的隔离状态，以真实 LocalSystem 身份通过 38 项服务目录断言，覆盖原字节保留、私有 runtime、嵌套句柄、故障拒绝和缺失目录零创建；一次性验证任务已移除。本机代理未修改，没有启动服务或 mihomo。
- [目录契约说明](../design/2026-09-07-installer-service-shared-roots.md)记录实现、探针故障注入和证据范围；收据为 artifacts/verification/shared-roots-system-validation-m4n.json。按用户最新要求，后续继续清理 Rust 安装器遗留，并完善简化 VS Installer 风格的 WPF 单页界面和自包含发布产物。

## M4o 验证记录（2026-09-07）

- WPF 安装器改为简化 VS Installer 的单页布局：一个产品卡片呈现状态及操作，执行时显示进度，诊断默认收起，底栏提供关闭。版本显示来自程序集或可信运行端口，继续固定 1.0.0；主程序仍为 WinUI 3。
- 保留现有状态决定的安装、修复、卸载及恢复操作，界面不增加权限。完成后重新检查会清除旧标题；关闭按钮复用取消并等待任务收尾的逻辑。状态和进度说明移除实现术语，卸载完成使用准确的通用完成提示。
- 完整 Release x64 构建 18 项目零警告、零错误；Presentation 97 项通过、零跳过；format 检查 1299 文件零处变更、无工作区警告。Core、Windows 和主程序未修改，本节点对应范围由 CI 继续回归。
- 编译后的实际 WPF 页面配合模拟运行端口通过 45 项离屏断言，生成 27 张状态/尺寸图并人工检查主要状态，无绑定错误；验证了较小内容区域、诊断滚动、执行进度及关闭等待。192 DPI 图像只是栅格输出，未进行真实显示器切换、UIA、讲述人或系统安装验收。
- 包括忽略目录在内，Rust/Cargo/Slint 文件与活动构建入口计数均为零；旧空 src、ui、tests 目录已删除，品牌参考图片保留。安装器绿色入口仍为自包含 EXE 加相邻离线 payload，正式执行开关未启用。
- [单页设计与截图](../design/2026-09-07-installer-single-page.md)记录职责、截图来源及验证边界。证据为 artifacts/validation 下的 1.0.0-m4o 日志，artifacts/verification 下的 installer-shell-validation-m4o.json 与 rust-installer-inventory-m4o.json；没有修改本机代理、SCM、证书或系统 ACL。前一节点 a4befb3 已同步远端且两项 CI 成功。

## M4o 发布产物复核（2026-09-07）

- `fad3d1b` 的两项 CI 完成，实际 TRX 合计 3678 项通过、零失败、零跳过，其中主程序 2330、Core 774、Presentation 97、Windows 477。
- 开发版归档 315923184 字节，SHA-256 为 `2b9a472de404d07979af3a8536b6a99490541a25d848c1f4bf78ccd903eadbaa`；实际解包为自包含 WPF EXE 与相邻离线载荷，共 7 个文件。它是 Development-Unsigned 开发产物，正式执行门仍关闭。
- 在没有全局 .NET 运行时的远端，清除子进程 SDK 环境及 PATH 依赖后，绿色 EXE 的载荷检查、大小写错误参数、混合模式参数和缺失载荷四种进程用例均得到预期结果，文件摘要保持不变。首次探针错误地要求解出 coreclr.dll，修正为 Windows 静态链接 CoreCLR 的实际打包方式后，在全新目录重跑通过。
- 收据为 `artifacts/verification/installer-artifact-m4o.json`、`ci-validation-m4o.json` 和 `installer-green-validation-m4o.json`。该检查没有调用 UI、安装、服务或证书修改路径。

## M4p 验证记录（2026-09-07）

- 新增独立的 Windows 机器访问权限迁移阶段。只接受 PreviousServiceRemoved，复核候选、双方 profile、固定机器布局及服务缺席，固定整个允许的目录树后才转移准确的旧账户 ACL；已完成的新 ACL 可重放，未知权限和目录成员直接拒绝。
- 机器载荷与服务数据共享目录转给新账户；Installer、InstallerAuthority 和服务私有边界保留原 owner/权限及文件内容。只定点读取旧 Installer/v2 的普通 Prepared 日志，持续阻止替换和写入，避免公共根权限切换后普通旧账户读取器无法继续验证的问题。
- 新增 62 项 Windows 测试，完整开发机安全子集 536 项通过、零跳过，三个证书存储修改测试仍仅在隔离 CI 执行。完整 Release x64 构建 18 项目零警告、零错误；format 检查 1307 文件零处变更、无工作区警告。
- 隔离远端四个进程共 65 项原生断言通过，包括真实 LocalSystem 私有目录、两处权限修改后的中断、新进程恢复、准确重放、文件防改名/写入、所有原文件字节及受保护目录完整安全描述符保留；一次性 SYSTEM 任务已清除，没有启动 SCM 服务或 mihomo。
- 三种额外继承故障夹具未能形成预期的“父项新 ACL、子项旧继承 ACL”：MAXIMUM_ALLOWED 与同卷移动得到受保护的旧权限，独占子句柄仍观察到新继承权限。它们的失败收据保留且不计入通过项；真实主路径自动传播已覆盖，强制逐子项补写分支仍由模拟端口验证。
- [机器权限迁移说明](../design/2026-09-07-installer-owner-transfer.md)记录实现和验证范围。主收据为 `artifacts/verification/machine-access-validation-m4p.json`。该阶段尚未接入完整迁移执行器和认证确认入口；不能据此宣称完整换绑或正式签名安装验收完成。

## M4p 发布产物复核（2026-09-07）

- `68def67` 已推送 main 并同步远端；[两项 CI 均成功](https://github.com/Water-Run/ClashSharp/actions/runs/34073420092)，实际四份 TRX 合计 3740 项通过、零失败、零跳过：主程序 2330、Core 774、Presentation 97、Windows 539。
- 1.0.0 开发版归档 315955773 字节，SHA-256 为 `e23f7e0eac480a7589c5d03222dc3d10f0e3501c1614d768ec111b4d89487d97`；7 个文件的摘要、EXE 版本和提交号均已核对。收据为 `artifacts/verification/installer-artifact-m4p.json` 和 `ci-validation-m4p.json`。开发版 EXE 没有在本机执行，正式执行门仍关闭。

## M4q 验证记录（2026-09-07）

- 新增关联文件迁移步骤和独立原生文件端口，要求已完成共享权限迁移、服务缺席及准确普通 Prepared；固定父目录和日志，短暂读取 association 后执行同目录原子替换。普通关联存储没有获得跨账户覆盖权限。
- 用已持久保存的事务 ID 绑定唯一恢复临时文件，只有准确新字节前缀及准确 ACL/单链接普通文件可以清理重建。替换确认丢失或取消后先等待实际状态复核，再报告成功、取消或 uncertain；未知证据保留。
- 新增 43 项 Windows 测试，完整安全子集 579 项通过、零跳过；18 项目 Release x64 构建零警告、零错误；format 检查 1312 文件零处变更，无工作区警告。
- 远端两个独立进程通过 16 个场景、332 项原生断言：恢复空白、部分及完整临时文件，处理已提交重放，拒绝异常归属/权限/链接及无关文件，并保留原文件和权限证据。所有树句柄释放，原 Prepared 可独占重新打开，没有遗留探针进程。
- 收据为 `artifacts/verification/association-transfer-validation-m4q.json`，设计和实际程序集摘要见[关联迁移说明](../design/2026-09-07-installer-owner-transfer.md)。该证据来自构造中断状态后的新进程恢复，没有进行实际掉电、完整认证 helper 或双 App 屏障验收。证书账本、Installer 权限迁移及确认入口继续实现。

## M4q 发布产物复核（2026-09-07）

- `0207d02` 已推送 main 并同步远端；[两项 CI 均成功](https://github.com/Water-Run/ClashSharp/actions/runs/34076177049)，实际四份 TRX 合计 3783 项通过、零失败、零跳过：主程序 2330、Core 774、Presentation 97、Windows 582。
- 1.0.0 开发包 315979164 字节，SHA-256 为 `ef58bff29b3d2bd36eda922a29639ef115d1059b33b5ad030698f10e9b5722ac`；7 个文件及 EXE 版本/提交号核对完成。收据为 `artifacts/verification/installer-artifact-m4q.json` 和 `ci-validation-m4q.json`，没有在本机执行 EXE。

## M4r 验证记录（2026-09-07）

- 新增纯 Core 证书账本状态策略及 Windows 迁移步骤：旧活动账本先归档，目标账本随后激活，观察准确活动副本后才清除其重复归档。两份原记录的身份、引用数、所有权和 generation 均保留，旧账户证书仍有归属依据。
- 活动账本留在旧 ACL 的 Installer/v2，私有归档按 SID 摘要固定命名，使用准确 SYSTEM/Administrators 权限。步骤持续固定普通 Prepared、新 association、共享根和私有 v1，不修改实际证书存储；错误和取消均等待整个文件后置条件复核。
- 新增 17 项 Core 与 40 项 Windows 测试，完整 Core 791 项、Windows 安全子集 619 项通过，零跳过；Core 行/分支覆盖率 93.46%/86.37%。18 项目 Release x64 构建零警告、零错误，format 1319 文件零处变更，无工作区警告。
- 远端全新隔离目录的 21 个场景、两进程共 657 项断言通过，包含实际 24 次文件操作、每个复制边界后的新进程恢复、完整重放、引用字节保留、异常文件/ACL/链接拒绝和句柄释放。没有修改证书、服务或代理。
- 首次占用文件探针误将原生拒绝码限定为 32，诊断观察到实际错误 5。修正探针并加入释放自身读句柄后同文件成功的对照，生产代码和权限不变；初次失败及诊断收据保留，不计入通过项。
- [证书账本迁移说明](../design/2026-09-07-installer-owner-transfer.md)与 `artifacts/verification/certificate-state-transfer-validation-m4r.json` 记录实际程序集和验证范围。Installer 状态权限、专用认证确认入口、旧账户独立清理和正式安装验收继续推进。

## M4r 发布产物复核（2026-09-07）

- 644ccfb 的 CI 34077718487 两项任务成功；下载的四份 TRX 合计 3840 项通过，零失败、零跳过。
- 自包含 WPF 1.0.0 开发包已下载并同步远端，压缩包 SHA-256 为 8c93d0c15d1f404e1411dbb818f6bfe46f8c8a998c74171e71abf00c3daa508e。远端七份文件摘要逐一匹配。
- 清除子进程 SDK 环境、限定系统 PATH 且没有机器级 .NETCore 运行时后，实际 EXE 的 --verify-payload 退出 0，并完成原生 WPF 依赖自解压。没有调用安装或系统修改路径，正式安装 gate 仍关闭。

## M4s 启动屏障、Installer 权限及完整阶段执行器（2026-09-07）

- 新增启动屏障步骤：检查旧账户完整固定目录与证书状态，仅创建缺失的准确普通 Prepared 或接受准确重放；旧版标记、未知临时文件、其他事务及异常文件均阻断。写入回执丢失后在持有目录句柄期间不取消地复核结果。
- 新增 Installer 权限步骤，只对 Installer、v2、Prepared 和可选活动证书文件申请修改 DACL 权限。支持全部 16 种准确新旧 ACL 组合；文件字节、共享机器目录及私有证书归档保持。最终观察只申请读取权限，要求全部目标 ACL、准确关联、普通 Prepared 和两账户证书归属。
- 六个生产步骤接入 WindowsOwnerTransferPhaseExecutor 与已有 Core 协调器。新增集成测试覆盖连续执行、每次阶段保存前的中断及 Verified 清除失败后恢复；始终保留普通 Prepared，步骤不自行写私有阶段或获取全局/双 App 排他权限。
- 新增 144 项 Windows 测试；本机安全子集 763 项、Core 791 项、Presentation 97 项、主程序 2330 项通过，合计 3981，零跳过。三个实际证书存储测试仅由隔离 CI 执行。18 项目 Release 构建零警告、零错误，format 1329 文件零变更，无工作区警告。
- 最终程序集在远端 14 个全新隔离场景、两个进程中通过 499 项原生断言，实际 Installer DACL 写入 12 次。验证了真实普通存储在启动屏障树下写入、一个受保护根修改后退出并由新进程续跑、目标用户普通读取 guard、准确重放零写入、文件写入/改名阻断及零遗留句柄。异常状态未修改权限，全部原文件字节和非迁移 ACL 保持。
- 最终原生 Windows DLL SHA-256 为 6934be83eafc2a6db4317e1e0822d9ff05a691afb50a3d321c84d59eddf35b30，Core DLL 为 378f5e39cc14ed00ecbe90192422d5e49acb835b9089f356d9baf8a924469353。补充账户路径后置复核前的一轮成功探针单独保留，不与最终 499 项相加。
- 原生验证仅操作隔离目录，没有修改服务、实际证书存储或代理。专用认证确认入口、全程双 App 屏障及普通安装接续尚待接入；这不是完整 GUI 换绑或正式签名安装验收。详细证据见 [迁移设计](../design/2026-09-07-installer-owner-transfer.md)及 artifacts/verification/installer-access-validation-m4s.json。

## M4t 专用账户确认与普通事务连续交接（2026-09-07）

- 已接通独立启动模式、认证 helper、只读 offer、会话绑定的明确确认、全部迁移阶段和普通安装/修复续接。凭据和私有日志留在 helper 内，普通跨账户标志不能调用这条路径。
- 迁移作用域连续持有全局、候选及两个 App 屏障，完成私有日志清除后才打开目标账户普通存储。父进程复用相同管道和提权进程；普通 broker 关闭等待 helper 实际退出。
- WPF 详情区增加“切换使用账户”，同页显示保留范围与两项明确决定。新增取消、并发关闭、认证失败、确认期间状态变化及恢复操作保持测试。
- 新增 92 项测试；本机全量安全集 4073 项通过，零失败、零跳过：主程序 2330、Core 822、Presentation 109、Windows 812。三个真实证书存储测试不在本机执行。
- 18 项目构建零警告、零错误，format 1347 文件零变更、无工作区警告。实际编译 WPF 的最终离屏验证为 83 项断言、35 张图，常规和最小窗口的完整确认区已目视核对。
- 日志位于 artifacts/validation 的 build-m4t-final.log、format-m4t-final.log、tests-m4t 与 installer-shell-m4t-final。最初的界面契约测试把确认边框误算为第二个产品实例，修正为按唯一产品语义校验后全量通过；早期渲染收据不累加到最终断言数。
- 完整管道组合验证中的 Windows 副作用与身份探测为模拟。生产执行编译门继续关闭；旧账户独立卸载、正式签名候选及 Windows 11 真实安装/故障矩阵未被这些证据替代。远端最近只读检查仍为零桌面会话。本机代理未修改。

## 现有配置验收进度

现有配置的隔离副本为 445030 字节，包含 32 个节点、3 个策略组和 9035 条规则，没有代理提供器。直接调用当前程序的运行配置生成器，普通代理与 TUN 两种覆盖均通过其内置语义验证，并保留上述集合数量。私有输入及生成配置位于被 Git 忽略的验证目录，输出报告仅包含数量和结果。

首次远端 `mihomo -t` 暴露隔离环境缺少 Geo 数据并尝试下载的依赖。补齐 Country、GeoIP、GeoSite、ASN 四项数据，并在两端分别核对 [MetaCubeX 发布](https://github.com/MetaCubeX/meta-rules-dat/releases/tag/latest)的固定 SHA-256 摘要后，两种运行配置均通过远端内核检查，退出码为 0，没有触发下载。上游发布 ID、资产 ID、字节数和摘要另存于本地验证收据；其 `latest` 标签本身不作为可复现发布的固定标识。此阶段没有改变系统代理或启用 TUN；节点联网和真实接管继续验收。

后续在远端启动独立 mihomo 进程，按原配置节点清单核对，32/32 个节点延迟探测成功，并通过仅供本次验证的 `127.0.0.1:17897` 代理转发 HTTPS 请求，得到预期 `204`。控制接口确认 TUN 未启用，结束后持有的内核进程已退出。测试没有修改系统代理；真实系统接管、完整 WinUI 操作和安装器安装验收仍单独记录，不能从这项独立内核测试推定。

随后在远端使用当前程序生成的 TUN 配置启用实际虚拟网卡。测试保留原节点配置，使用唯一测试设备名，并增加 4 项已建立管理连接的路由排除。启用前登记只针对本次进程和设备的两分钟恢复任务；启用后网卡为 Up，出现 127 条测试路由。显式禁用 HTTP 客户端代理的 HTTPS 请求得到 `204`，控制接口同时观测到使用所选节点链路的 Tun 连接，证明请求经过真实 TUN 转发。

测试结束正常关闭自身内核和 TUN，确认自有路由剩余 0，再移除临时恢复任务。另一次只读复核确认没有活动测试网卡、恢复任务或测试端口监听，SSH 仍连通。本机代理和远端系统代理均未修改。脱敏收据为 `artifacts/verification/live-tun-validation-1.0.0.json`；这项结果覆盖独立核心的实际转发与正常退出清理，尚不覆盖主程序服务接管、GUI 导入、强制崩溃恢复和签名安装候选的故障矩阵。

远端仍没有已登录的桌面会话，已准备本机环回 SSH 隧道与远程桌面连接文件，等待用户完成 Windows 登录；当前安装器的 Windows 11 客户端平台约束仍未更改。

各里程碑记录实际构建、测试、UI、机器副作用和产物证据；源码完成与正式发布条件分别判定。详细基线见 [2026-09-06 开发状态](2026-09-06-development-status-and-next-steps.md)。

## M4u 旧账户归档证书清理（2026-09-07）

- 完成独立的归档读取/释放/清除能力及 Windows 精确证书删除适配器；只能清理已认证旧 SID 的既有归属，不持有证书导入、共享服务或关联修改能力。详见[旧账户独立卸载](../design/2026-09-07-installer-retired-account-uninstall.md)。
- 新增 95 项测试；本机安全子集 4168 项全部通过，18 项目 Release x64 构建零警告、零错误。Windows 三个真实证书修改测试仍由隔离 CI 执行。
- 隔离远端两个进程通过 13 个场景、84 项原生断言；真实证书存储恢复到测试前状态，四张测试证书、所有句柄和探针进程均清理，测试目录已经移除。
- 本阶段尚未接通旧账户独立卸载的认证会话、恢复日志和 WPF 操作。探针中的包缺席与旧账户授权边界为模拟，不作为完整卸载或正式签名发布验收。1.0.0 生产执行门保持关闭，本机代理未修改。

## M4v 旧账户独立卸载完整入口（2026-09-07）

- 连接专用启动模式、认证管道、helper 私有恢复记录、父进程包卸载与 M4u 证书清理。五个阶段可恢复；挂起的旧账户卸载阻断普通安装和账户迁移。只持有旧账户 App 屏障，共享服务与新账户应用保持。
- WPF 详情区提供明确确认和独立恢复入口，取消先于后端调用，进度及错误使用副本卸载文案。实际编译页面通过 133 项离屏检查、45 张渲染；没有启动正常程序或安装运行时。
- 新增 127 项测试，本机安全子集 4295 项通过，零失败/跳过；18 项目 Release 构建零警告/错误；format 1381 文件零处变更。完整协调器与管道集成验证了包删除后取消、释放作用域及新会话恢复。
- 隔离 Windows 两进程通过 16 个场景、60 项原生断言，232 个目录打开全部释放，测试树清理完成。该原生探针验证文件及 ACL；账户路径与服务缺席前提为模拟，没有实际包、证书、服务或代理修改。
- 详细实现和准确程序集摘要见[旧账户独立卸载](../design/2026-09-07-installer-retired-account-uninstall.md)。正式签名 Windows 11 安装矩阵及交互式 WinUI/WPF 验收继续推进；1.0.0 生产执行门保持关闭。

## M4w 候选依赖说明与最终 MSIX 绑定（2026-09-07）

- 新增离线生成器，从四个生产项目的还原图和锁文件建立依赖说明。当前覆盖 61 个 NuGet/SDK 输入与 90 份原始文档，包含许可、通知、包元数据和固定 GeoData 生成资料；不会把构建机用户目录或缓存路径写入清单。
- 使用固定 SDK 的 NuGet 实现校验签名包内容完整性及锁定内容摘要，另记录实际归档 SHA-256。SDK 下载依赖有独立精确内容摘要；缺少包内许可原文的已知依赖使用按包名、版本和声明匹配的来源快照。生成过程不下载、不修改缓存或证书。
- 相同输入两次生成的 ZIP 字节一致。构建将同一份说明 ZIP 放入最终 MSIX 和安装器目录，重新读取实际 MSIX 校验内部副本，并将摘要与长度写入受载荷清单保护的 provenance。
- 30 个场景、53 项契约断言通过，包含真实签名 NuGet 包的正常处理与被修改内容的拒绝，以及依赖漂移、许可缺失、源摘要错误、路径异常、归档缺失/重复/篡改和 MSIX 内部副本错误。15 个 PowerShell 源文件在 5.1 和 7 中解析通过；既有打包运行库的 1 个有效/19 个拒绝样例通过；18 项目 Release x64 构建零警告、零错误。
- GeoData 动态上游输入尚未完整重建；这些资料不等于全部数据许可或 mihomo 完整对应源代码的发行归档。详见[候选依赖说明](../design/2026-09-07-installer-third-party-notices.md)。生产执行门保持关闭，正式签名与 Windows 11 完整验收继续推进。

## M5a Windows 11 实际包安装与窗口启动（2026-09-07）

- 使用本机已有 Sandbox 在独立 Windows 11 客体中执行真实验证。输入只读、结果目录单独可写、客体断网，主机按预先保留的会话 ID 管理生命周期。本机没有包、证书、服务或代理修改。
- 当前 3282a03 候选通过实际依赖/MSIX 安装、精确包注册、WinUI 进程包身份、EXE 摘要和 30 秒主窗口检查；客体中的测试包、证书、载荷及新增依赖已清理，会话已销毁，主机代理及输入摘要不变。
- 重写输入和报告契约，103 项断言在 PowerShell 5.1 和 7 中通过。旧报告、标识错配、部分执行、代理变化及缺少清理均被拒绝；窗口观察时长不得超过对应步骤的实际时长。
- 18 项目 Release x64 构建零警告、零错误，16 项仓库架构检查通过；format 检查 1381 文件零处变更。
- 实际发现开发证书仅导入 CurrentUser/TrustedPeople 时 MSIX 部署失败（0x800B0109），改用客体机器 TrustedPeople 后同包成功。需要继续修正安装器的开发签名信任流程及其机器归属/恢复语义；用户证书单元测试不能替代这项验证。
- 本阶段不构成完整页面交互、正常退出、WPF 完整卸载或正式签名发行验收；未实现的全矩阵场景继续拒绝，生产执行门保持关闭。详见 [Sandbox 包验证](../design/2026-09-07-sandbox-package-validation.md)。

## M5b 机器证书归属、真实签名校验与范围隔离（2026-09-07）

- 将独立的机器 TrustedPeople 归属、持久化及恢复能力接入认证 helper，与用户证书和旧账户授权解耦。已有证书保留；自有证书在没有同发布者包引用时才精确删除，异常状态保留恢复记录。
- 原生验证发现用户逻辑证书库会继承机器证书，改为固定物理注册表存储；当前用户、目标用户和归档用户删除入口都有隔离回归。修复 WinVerifyTrust 状态释放过早及管理员检查缺少令牌复制权限的真实问题。
- 18 项目 Release x64 构建零警告/错误，本机安全子集 4370 项全部通过；另六项真实证书修改测试留在隔离环境。format 检查 1394 文件零处变更。
- Windows 11 客体通过 20 个独立进程、138 项断言，包括真实 MSIX 引用保留、组合安装/修复/卸载、四个进程退出点及重启恢复。十项客体清理检查和主机的会话销毁、输入不变、代理不变检查全部通过。
- 客体测试临时收紧并最终恢复卷根一条精确 ACL；生产策略未放宽。一次过宽的本机测试筛选误执行临时用户证书往返，已清理并记录，随后加入测试环境限制。详细证据和未覆盖边界见[机器证书信任](../design/2026-09-07-installer-machine-certificate-trust.md)。正式签名 WPF 安装及完整机器故障矩阵继续推进，1.0.0 生产执行门仍关闭。

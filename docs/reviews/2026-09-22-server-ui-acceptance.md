# Windows Server 实机功能与界面验收

验收进行中，尚未通过完整发布验收。覆盖范围包括所有页面、设置、动态对话框、正常及异常操作、取消与重试、状态恢复、窗口与主题，以及安装、修复、卸载。页面能够打开、单元测试通过、安装器能够启动，都不能代替这些使用流程的实际验证。

## 环境与基线

Windows Server 2025 Desktop Experience，系统版本 10.0.26100，使用临时隔离测试账户。通过 SSH 跳板上的私网 RDP 会话、截图和键鼠操作执行真实桌面验收，没有开放公网远程桌面。每次输入后重新获取截图。

首个候选来自主线 `c4ed46e4051293823359c4fc8bb50cb5152a0e80` 的源码归档。使用固定 SDK 10.0.201 和固定离线输入，在目标服务器构建正常安装器，启用生产安装运行时，以临时验收证书签名并加时间戳。该候选仅用于内部验收，不是正式发行签名包。

| 产物 | SHA-256 |
| --- | --- |
| 源码归档 | `859A409CD20780F013A2A1CBA5C73BC9C4BE41F71EADDD4BBBEDCD3CD7681196` |
| 安装器 | `7D6960B6D0DDEDB43C433CB6387029703DE028FCD74CA7809006E47602AE6255` |
| MSIX | `5D62D8490F6358D5F34642001932F149782A81DE126A9D3A66F31133AD328EEC` |

安装前，该测试账户没有产品包，目标产品服务和六处产品目录均不存在。安装后读取实际包注册、服务及目录状态，保留安装前后快照。

## 已执行流程

- 安装器默认窗口、展开详情、最大化、点击安装、完成、重新检查、正常关闭。安装后账户包注册成功，产品服务运行，六处目录创建。
- 应用正常首次启动、启动引导、默认浅色、切换深色、最大化、主控与关于页面。
- 使用原生文件选择器导入本地配置，校验，设为当前，并在节点页读取运行时策略组。
- 待命模式下，真实 HTTP 和 SOCKS5 入站请求均直连，原生用户系统代理保持关闭。
- 按规则接管后，真实 HTTP 和 SOCKS5 入站请求均通过测试代理；在节点页将规则组切换为 DIRECT 后，实际请求立即直连。
- 持续传输在连接页显示目标、路径、规则和流量；统计页聚合配置、日期、节点数据；从统计页打开系统日志。

网络验收使用服务器回环地址上的受控 HTTP 源站和代理。代理仅允许自己的测试源站；同时支持 HTTP 转发及 CONNECT。最初只支持转发的夹具被 mihomo 的 CONNECT 请求拒绝，此阶段失败不归因于产品，修正夹具后重新验证并保存了新的结果。

## 第一批发现与修复

以下源码修复已加入本轮候选，目标服务器的新包复测尚未完成。

| 问题 | 修复与复测要求 |
| --- | --- |
| 系统代理界面显示开启，原生用户注册表仍为关闭 | MSIX 清单仅将当前用户 Internet Settings 键排除写入虚拟化；新包必须验证原生代理生效、退出恢复及卸载后无遗留 |
| 首页 SVG 只显示白色图形，缺少绿色底图 | 使用已有的高分辨率 PNG 标志；复测浅色、深色和缩放 |
| 应用深色、系统浅色时，原生窗口按钮黑色不可见 | 跟随实际 XAML 主题更新原生标题栏主题；复测切换、悬停及非活动状态 |
| 校验配置后选中项丢失，随即设为当前没有作用 | 配置及订阅按稳定 ID 恢复新行选择；无目标时禁用相关按钮；复测连续校验、应用、编辑、删除 |
| 当前配置显示内部 ID，磁贴换行后文字被裁切 | 运行时快照带回配置名称并核对 ID；磁贴单行省略并提供完整值提示 |
| 模式切换期间长时间没有忙碌反馈 | 显示进度并阻止并发模式切换；复测成功、失败、取消后的恢复 |
| 资源更新时间显示 0001 年 | 未报告有效时间时显示占位符 |
| 规则目标名称直接裁断 | 增加省略与完整内容提示 |
| 配置、订阅、规则列表高度无约束；节点页缺少整体滚动 | 列表分配剩余高度，节点页提供滚动并限制内嵌列表高度；需大量数据和较小窗口复测 |
| 安装完成后详情仍显示安装前状态 | 失效就绪快照时同时清空陈旧能力行；重新检查后显示最新结果 |

MSIX 注册表排除依据微软的 [Flexible virtualization 文档](https://learn.microsoft.com/en-us/windows/msix/desktop/flexible-virtualization)。原生标题栏主题使用微软的 [AppWindowTitleBar.PreferredTheme](https://learn.microsoft.com/en-us/windows/windows-app-sdk/api/winrt/microsoft.ui.windowing.appwindowtitlebar.preferredtheme?view=windows-app-sdk-1.8)。

## 第二批实测与修复（9 月 23 日）

- `b5bb453` 的两项 CI 作业均通过；目标服务器也完成该提交的生产安装包构建。因继续发现退出阻塞问题，尚未安装此中间候选。
- 日志分类选择后仍显示原来的全部记录。搜索及两类筛选的双向绑定抢先更新了 ViewModel，使事件处理器误判无需加载；改为由事件负责更新，并在数据源变化后恢复筛选选择。离开连接页的 WebSocket 取消只在所属传输边界归一化，保留真实传输错误。
- `c4ed46e` 两次托盘安全退出均失败，随后点击未启用模式也失败。日志为 `mutation-compensated`，应用仍运行，内核被补偿逻辑重新启动。源码定位到目标状态仍保留正在使用的 ClashSharp 代理地址，而释放代理实际还原了原始地址，验证因此误判。修复以所有权日志预测释放结果，涵盖空地址、原有关闭代理、原有启用的其他代理和旧版端点；退出失败时恢复窗口并显示可关闭的提示。
- 为完成旧包维护验收，记录现场后对精确核验过的测试应用进程执行受控崩溃。应用与内核均退出，代理所有权日志自动清除。此项不计作正常退出通过，也不能代替新包原生系统代理恢复测试。
- 使用安装器点击修复完成，随后点击卸载完成。外部快照确认产品包、服务和六处产品目录均为零，安装器管理的测试发布者信任证书已移除，系统代理关闭。一次取消按钮点击发生在修复已经结束之后，不计作取消流程覆盖。

第二批网络还原、日志与传输修复完成后，18 项目构建零警告零错误，主程序 **2985 / 2985** 回归通过，格式检查通过；随后增加退出失败提示，最终构建仍为零警告零错误，相关界面、架构与本地化检查 **438 / 438** 通过。所有第二批修复仍需新包实机复测。

## 新候选实机复测与第三批界面问题

`47aab42a36cfc06933f795c9c37713269224703f` 的[两项 CI](https://github.com/Water-Run/ClashSharp/actions/runs/35753156640) 均已通过。目标服务器使用同一源码重新构建、签名并安装；内部验收安装器 SHA-256 为 `0D974B8D870F44D1CB8749B6A02F68D23CF769406CD2F62D0C4DD5CDE1E37B97`，MSIX 为 `8DEF9DA4A92765988D50E3687369F74FA0A4CF52B264EC275471A945E24EC9AC`。

- 实际点击安装后取消，重新检查显示待继续；关闭安装器再打开同一个候选，继续安装成功。尝试使用旧候选继续时，事务正确拒绝，但说明未指出应使用原安装器，已修正文案，待新包复测。
- 导入测试配置后，连续点击校验和设为当前成功；校验后仍有可见选中状态。主控显示配置名称，绿色图标完整。
- 关闭 TUN 偏好后按规则接管，原生用户注册表实测为 `ProxyEnable=1`、`127.0.0.1:10000`。在同一交互用户下使用 WinINet 的系统代理设置请求受控域名，源站报告实际经过测试代理。该域名只存在于 mihomo 测试配置中，关闭代理后 WinINet 恢复预期 DNS 失败，未修改系统 hosts。
- 点击未启用模式，内核退出，原生代理恢复 `ProxyEnable=0` 且代理地址不存在。再次接管后，从托盘安全退出，应用和内核均结束，代理再次恢复；随后正常重新启动并恢复先前的模式与 TUN 偏好。
- 日志分类和级别筛选实际生效；组合搜索只返回匹配记录，刷新保持条件，清空搜索恢复结果。实机仍发现组合框选中文字消失，已加入显式条目模板，仍需新包验证；空结果原先只有空白列表，现已接上已有本地化空状态提示。
- 配置列表补充列名及长名称完整提示；磁贴开关增加清晰轨道与关闭颜色，TUN 磁贴将偏好值和实际运行状态分开说明。启动提示此前将重启后正常恢复的当前代理误报成残留，现仅在没有正在运行且持有该代理的应用内核时报告残留。

第三批修改完成后，18 项目 Release x64 构建零警告、零错误；界面、日志、启动检查、资源与架构相关 **659 / 659**，Installer Presentation **144 / 144**，均无失败或跳过；变更文件格式检查通过。第三批源码修复不能替代下一候选的真实界面复测。

## 第三批候选安装与第四批修复

`0b880e28ce69837705b11e061fe6c50894a9738e` 的[两项 CI](https://github.com/Water-Run/ClashSharp/actions/runs/35757524539) 均通过。服务器重新构建的安装器 SHA-256 为 `65633A26242B1F440A3123D319E38EC5BDF0EF9488BD4E238ADF33F147285AE7`，MSIX 为 `0A47D1551B290C7A17D02B689760248CF3671620C8EB4D27EB4EF0925DCBF3A6`。

- 程序运行时，新安装器正确禁止维护并要求关闭应用。通过托盘安全退出后，应用与内核均结束，原生代理关闭，重新检查恢复修复/卸载入口。
- 不同候选仍使用同一包版本 `1.0.0.0`，直接修复被 AppXSVC 以 `0x80073CFB` 拒绝；事件明确报告相同标识、不同内容。此为 [Windows 的包标识约束](https://learn.microsoft.com/en-us/windows/win32/appxpkg/troubleshooting)，不能计为升级成功。安装器此前只有笼统的继续提示，第四批将此错误映射为稳定诊断并解释直接重试无效。
- 此次使用外部辅助恢复：确认产品仅注册在临时测试账户，备份并逐个校验 14 个 LocalState 文件，在该用户上下文移除旧包、注册精确的新候选，再通过原安装器继续原修复事务。没有编辑或删除事务记录。恢复后核验安装程序集与候选包相同，事务正常清除。此流程不计为安装器自动恢复或偏好保留；卸载已重置 LocalSettings。
- 重新恢复测试业务数据后，用相同的新候选再执行一次正常修复，界面成功、事务清除，14 个数据文件哈希均未改变。随后启动应用，重新在配置页选择恢复的测试配置；首次尝试切换 TUN 前，旧运行态和重置后的偏好不一致，界面报错，重新选择配置后恢复。该人为恢复路径与普通修复分开记录。
- 新包实测配置列名、未选中时禁用目标按钮、TUN 偏好/运行状态说明和关闭开关轨道均可见。主控标志与配置名称正常显示。待命模式 HTTP、SOCKS5 均实际直连，原生系统代理关闭。
- 深色窗口中的启动提示仍使用浅色对话框及行背景。第四批改为在窗口对话框协调器继承实际主题，检查行使用 XAML 主题资源。此前疑似无法滚动的末行，经在内容区滚动后可达，Escape 也能关闭，不再列为已确认缺陷。
- 安装器所有状态原先都使用绿色，包括失败；第四批将状态标签改为中性色，保留明确状态文字及高对比度系统资源。
- 全局接管选择测试节点后，HTTP 与 SOCKS5 请求均实际经过受控代理，原生代理保持开启。日志的无匹配搜索明确显示空状态，清理预览从按日期的零条变为按级别的五条，Escape 取消后记录数量不变。
- 显式日志条目模板没有解决初始筛选标题空白，第四批改用索引绑定，并在类别列表变化后重新发布选中索引。回归覆盖新增类别改变位置后仍使用原筛选。清理对话框在最大化窗口中偏左，定位为直接限制了弹窗宿主的 MaxWidth；清理和选色均改为设置 ContentDialogMaxWidth 资源，清理说明文字改为继承弹窗主题。

第四批构建零警告零错误，日志、界面资源与架构 **618 / 618**、安装器界面 **145 / 145**、包适配器 **22 / 22** 均通过且无跳过，变更文件格式检查通过。主题、筛选标题、弹窗居中和冲突提示仍需下一候选实测。

## 备份导出实机故障与第五批修复

`5279d29` 的[两项 CI](https://github.com/Water-Run/ClashSharp/actions/runs/35762928427) 均通过，服务器生产安装包也已构建完成。尚未安装该中间候选时，继续操作 `0b880e2` 的真实备份流程发现阻塞问题：规则接管、内核运行时导出“设置和代理配置”，保存选择器创建了零字节 XML，导出异常仅写入日志，页面没有结果提示。

日志确认 `mihomo/cache.db` 被正在运行的内核独占；该缓存不应作为可迁移配置读取或还原。第五批修复统一排除运行缓存，也让旧备份中的缓存条目不覆盖当前内核缓存。XML 导出先完整写入同目录暂存文件，再替换目标；替换失败保留原备份并清理暂存文件。共享导出流程加入七种语言的完成/失败对话框，文件选择取消不显示成功。

新增回归覆盖独占缓存时导出、旧备份不覆盖独占缓存和目标占用时保留旧文件。18 项目构建零警告零错误，备份、本地化和界面资源相关 **486 / 486** 通过、无跳过，变更文件格式检查通过。新包仍需实测运行中导出、真实结果提示、取消和还原流程。

## 第五批候选实测与第六批修复

`e27b2a38e2071a21c3d7889366f763fc499f256f` 的[两项 CI](https://github.com/Water-Run/ClashSharp/actions/runs/35764726712) 均通过。服务器安装器 SHA-256 为 `66A6E64CAE3409CB8E69D0BFAE43F54D715D9C9B3C5D332B75E3AF30B0D6AAC6`，MSIX 为 `A3AE96DDF9882200CC7CC547B347E3FA21D1C30FA615BD9D144562FC8586B0EB`。

- 在 `0b880e2` 停用内核后，实际导出有效的设置和代理配置 XML，保留 31 项设置和测试配置。通过设置页正常退出，再使用旧安装器正常卸载；外部快照确认包、服务和六处产品目录均已清除，测试发布者信任已撤销，原生代理为关闭且地址不存在。
- 使用新安装器全新安装成功，随后核验安装程序集与 MSIX 内程序集 SHA-256 相同。安装器建立所需信任后包签名有效；安装前的未知签名状态是刚完成卸载后缺少临时发布者信任，未放宽系统验证。
- 深色启动提示的行背景与文字已正确跟随主题，Escape 可关闭；日志清理对话框现位于窗口中央，深色说明和预览文字清晰可读。级别筛选标题正常，类别筛选在新服务日志来源到达后仍变空；第六批改为原位更新可观察选项集合，保留原生 ItemsSource 与存续条目，避免更换集合清空选中项。
- 通过真实文件选择器还原刚才的有效备份，两次均失败并回滚，没有结果提示。日志记录 `service.unavailable`，但服务正常、待命及停用成功。源码定位到导入先发布新的当前配置偏好，随后旧网络适配器把旧运行配置与新偏好比较，误判基线未知。第六批让独占导入/重置流程使用独立核验的实际网络配置事务，显式切换目标配置；普通模式切换路径保持原有检查。新增生产运行时回归涵盖停用、待命、规则接管下切换到还原配置并恢复原配置。
- 共享导入流程增加七种语言的完成、无效备份和失败结果提示；无效范围在写入前返回，失败保留诊断记录，取消不显示成功。

第六批 18 项目构建零警告零错误，运行配置、导入、设置、日志、架构及资源相关 **887 / 887** 通过且无跳过。第六批仍需新包实测；第五批运行中导出结果也仍待实测，不能用源码回归代替。

## 第六批候选实机验收与首页刷新缺陷（9 月 23 日）

`b916f7ec60a0a732d7b5ce4180efe64592dbe852` 的[两项 CI](https://github.com/Water-Run/ClashSharp/actions/runs/35768479137) 均通过。服务器构建的安装器 SHA-256 为 `1CC36779FBB95A362CCBD3F8587D0D24C6693711D8F6E5FB295FD9EA8B9B7006`，MSIX 为 `5FB8F4588642280CC14A91C5FF27656AE8D91D529263C26CA4695051537EE908`。旧 E27 候选经界面正常卸载，外部快照确认包、服务、六处产品目录和安装器管理的信任证书已清除，原生代理关闭；随后新候选正常安装，安装程序集与包内哈希一致。

- E27 的选色弹窗已居中、遵循深色主题。取消绿色选择后仍保留原蓝色；确认绿色并重启后生效，待重启标记消失。基础组重置的默认回车执行取消，再显式确认才还原默认偏好。
- B916 通过原生文件选择器导入旧有效备份，显示“导入完成”，三份代理配置文件逐个哈希一致，旧 `cache.db` 不还原。切换规则接管后，HTTP 和 SOCKS5 请求均实际经过测试代理，原生代理为 `127.0.0.1:10000`。
- 在内核运行时导出“设置和代理配置”成功，XML 含 31 项设置与 3 份文件，输出 SHA-256 为 `84D2B647881060E37AB77D7D9B496ECACDE44E9FF48AA4D9AB85E8EA980CDAF7`，没有遗留临时文件，内核继续运行。取消保存选择器返回设置页面，没有伪成功提示；损坏的 XML 在确认前被明确拒绝。
- 日志页初次加载、离开再进入、选择设置类别及刷新时，筛选标题均保持可见，过滤结果正确。
- 连接页真实显示持续下载的目标、路由及增长字节数，无匹配搜索显示空状态，清空搜索恢复列表。单条关闭使 60 秒测试下载在约 21 秒时以 curl 18 结束；全部关闭使两条并发下载同时以 curl 18 结束，列表清零。自然完成后才点击关闭的早期尝试未计为通过。
- 统计页从空流量变为有流量、配置、节点及采样计数；这些是采样数据，尚未证明与完整传输字节数相等，也没有计为完整统计验收。
- 首页切换模式后当前节点仍显示不可用，离开再回来才更新。在页面停留期间进行 60 秒实际下载，首页一直显示 0 B/s、0 个连接和旧总量；外部核验此时 curl 仍运行且已写入 188416 字节，最终下载 245760 字节。第七批增加页面可见期间的刷新和离开时的取消、排空；每秒读取内存流量，存储摘要仍限频，核心版本探测单独限频。模式切换会使此前的异步查询失效，防止旧结果覆盖新状态。源码修复仍需新候选实测。

第七批本机 18 项目 Release x64 构建零警告零错误，主程序 **3003 / 3003** 回归通过且无跳过，变更文件格式检查通过。首页刷新相关的 43 项定向回归也已通过。开放 PR 仍为零，本地与远端开发分支仍仅 `main`。

B916 后续备份验收：取消文件选择器、首次确认及二次确认均回到设置页；两次确认的默认回车都执行取消，六份配置文件哈希与内核 PID 不变。运行中 SQLite 导出成功，`PRAGMA quick_check` 为 `ok`，含 73 条日志、15 条连接记录和 64 条流量快照，文件为 73728 字节。不兼容的 `Version=999` 备份未改变配置或内核，但经过两次确认才显示笼统失败；第八批使 UI 预检查复用导入的根元素、格式、版本和范围校验，在确认前显示已有无效备份提示。相关 **196 / 196** 回归及变更文件格式检查通过，尚待新包实测。

## 第七批候选实测与第九批修复

`ebbd9542dd3798f2b0b96ff96cba5e995a1c87f8` 的服务器安装器 SHA-256 为 `4E211319CC4CEF597822244785C3470F827B2B64B6BE942E89F109A1B843EEE1`，MSIX 为 `06B621B97B4684A457EEB49784391FEFE6187C3BBE72E5650D8AAFC45BE4AC1A`。B916 经界面正常卸载，外部快照确认包、服务、六处产品目录及安装信任清除、原生代理关闭；EBB 全新安装成功，安装程序集与包内哈希一致。EBB 的 CI 被下一次提交取消，不计为通过；第八批 `3cdf89b` 的[两项 CI](https://github.com/Water-Run/ClashSharp/actions/runs/35806796018) 均通过且服务器构建成功，未单独安装。

- EBB 导入运行中的规则接管备份成功，实际 HTTP 请求经过受控代理。首页停用、恢复规则接管时，当前节点现能自动变为不可用、再恢复实际节点，无需离开页面。
- 同一首页进行 60 秒、245760 字节下载时，速率和连接数仍为零。定位为共享流量采样器只有依赖运行态的触发器才调用；第九批首页直接触发带取消与超时的采样，并合并一秒内的并发读取，避免不同消费者把同一个计数差值变成假零速率。累计流量仍是采样累计，不能宣称已验证全量字节精度。
- “设定显示”八个下拉框初始及恢复默认后均无选中标题，手动选择只显示当前一个。第九批改为原生索引绑定，保留重复项目交换与默认布局恢复。
- 磁贴搜索能匹配标题及说明，勾选版本磁贴后取消不改变首页，重新勾选保存后显示实际 mihomo 版本。但固定内容宽度超出弹窗，导致说明和滚动条被裁剪；无匹配搜索只显示空白。第九批统一弹窗内容约束，并为可搜索选项补齐七种语言的空结果提示。

保存磁贴后通过设置页执行重启，EBB 正常恢复规则接管，版本磁贴仍保留。第九批最终 18 项目 Release x64 构建零警告零错误，主程序 **3014 / 3014** 回归通过、无跳过，全部变更 C# 格式检查通过；显示选择器相关 **367 / 367** 也已通过。新包实机复测仍待完成，所有源码结果均不替代实机验收。

## 第九批候选实测与累计流量漏计

`1924e6116edff562272f53d9d382e736428e8f54` 的[两项 CI](https://github.com/Water-Run/ClashSharp/actions/runs/35808631603) 均通过。服务器安装器 SHA-256 为 `25675ED25E5DC18C17EF22CAF0031391BDF5FC78E5EC67657D0E5A7E8C936859`，MSIX 为 `9714CFB42C0C2E0C484043BF2C75A70425BE4C565043E8EEB3C6A14A46617115`。EBB 正常卸载后包、服务和六处目录清零、系统代理关闭；1924 正常全新安装成功，安装程序集与包内 SHA-256 均为 `4E0622311EAB5F3DCACC6C4BF82E635B7573FCF0711F45F22AF18889A4DC56C0`。

- 有效备份正常导入，恢复规则接管与深色主题。无运行态触发器的首页实测持续下载显示约 4 KB/s、1 个活动连接，结束后自动归零，期间没有导航刷新。下载在 02:13:11 至 02:14:12 UTC 完成，curl 退出码 0、响应体 245760 字节。
- 累计统计仍有明确漏计：同次下载的首页历史总量仅为 160.58 KB，本次运行磁贴下载量为 232.64 KB，均低于 240 KB 响应体。前者来自持久化连接采样，后者来自活动连接差分；两者都不能覆盖采样间关闭的连接及末尾字节。这是单独待修缺陷，速率刷新通过不代表累计统计完整。
- 首页八个显示选项在浅色和深色主题下都有正确初始标题；重复选择交换对应位置，实际重启后保留，恢复默认后全部标题仍可见。
- 磁贴长说明、类别与滚动条恢复可见；无匹配搜索有明确提示。筛选隐藏已勾选项目后保存仍保留这些项目。内存磁贴拖到第一位后，实际重启仍保留顺序。在约 872×592 的较小窗口中，弹窗标题、搜索框和保存/取消可见；取消隐藏核心磁贴不改变首页，重新隐藏并保存后磁贴移除。
- 不兼容的 `Version=999` 备份现于两次确认前直接显示“无法导入此备份”。十份配置/状态文件哈希不变，运行中的内核 PID 保持 13556；没有写入或重启副作用。

以上原始截图及下载、安装哈希、不兼容导入核验结果已归档。TUN、订阅、触发器、完整统计及异常恢复等覆盖继续进行，未计为完成。

## TUN、全局接管与节点状态验收

1924 候选在实际服务器上完成四种模式与 TUN 开关的第一轮网络检查。为排除回环地址绕过 TUN 的假阳性，受控代理只额外接受测试地址 `203.0.113.10:18080`，并将其映射到验收 HTTP 服务；客户端使用 `--noproxy '*'` 发起请求。

- 规则接管与全局接管的 TUN 请求均被受控代理实际转发，Meta 网卡和默认路由存在，系统代理关闭。服务持有内核时按设计不开放 HTTP/SOCKS 混合端口。
- TUN 关闭后，规则和全局模式的 HTTP、SOCKS5 以及测试用户的 WinINet PRECONFIG 请求均通过受控代理，系统代理为 `127.0.0.1:10000`。全局测试先明确选择 GLOBAL 代理，避免将默认 DIRECT 误判为代理失败。
- 待命撤销系统代理和 TUN 路由，内核保留，HTTP/SOCKS 测试走直连。停用后内核和代理端口关闭，系统代理及 TUN 路由撤销。TUN 偏好在待命和停用时保留，但界面明确显示当前未运行。
- 发现节点显示缺陷：全局模式中 GLOBAL 选择 Acceptance HTTP，规则组选择 DIRECT，网络请求实际走代理，但首页当前节点及延迟磁贴显示 DIRECT。源码修复按已确认的运行模式选择组、展开嵌套组、拒绝循环，并在待命显示 DIRECT；读取前后核验同一个配置版本，模式切换期间不发布过期节点。
- 发现独立持久化缺陷：从上述 TUN 状态关闭 TUN 后，GLOBAL 静默回到 DIRECT，规则组也恢复初始选择，导致真实请求改为直连。该问题尚未修复，不能把模式组合计为完整通过。

原始截图归档截至 02:45 UTC，共 537 个文件，压缩包 SHA-256 为 `3B4D274B21DB43F7D7A4749A09244C99A51DF3F12BE5E62F1D7C3C3B23D3FF2F`。一次测试服务随 SSH 进程结束、一次探测与模式切换重叠的结果均保留为无效尝试，结论使用重测后的独立证据。

节点状态修复验证：18 项目 Release x64 构建零警告/错误，主程序 **3029 / 3029** 通过、无跳过；节点状态、主控及架构相关 **270 / 270** 通过，变更 C# 格式检查通过。尚未安装该修复候选；节点选择持久化和累计统计仍待修。

## 节点选择持久化修复

第十一批将内核确认后的节点选择按配置保存，写盘失败或取消时恢复并核验原选择；切换模式及 App/服务所有者时，在提交系统代理之前恢复该配置的选择。启用内核选择缓存以支持同一所有者的进程恢复，同时对没有保存选择的手动组恢复配置默认项，避免同名组继承另一个配置的缓存。订阅已删除的节点不再发送给控制端，恢复未获确认会阻止配置事务提交。

18 项目 Release x64 构建零警告/错误，主程序 **3049 / 3049** 通过且无跳过，变更文件格式检查通过。期间全量回归发现测试单例装配及异步归属检查问题，均修正后整组重跑；这些结果仅证明源码回归，新包实测仍待进行。

同时继续检查 1924 的订阅页：空 URL 提交直接关闭弹窗，没有用户可见错误；完成操作后无选中项的检查、更新、编辑、删除按钮却全部启用。截图 `desktop-20260923T031215Z-829732953.png` 和 `desktop-20260923T031235Z-561775490.png` 保留原始行为，作为后续界面修复及复测项。

## 订阅输入反馈与按钮状态修复

1924 实机完成有效本地订阅添加、HTTP 200 检查及更新，选中项在检查和更新后保留；新增条目的时间显示为 `0001/1/1`，更新后的原始时间又超出固定列宽。第十二批让无效名称、URL 和非整数更新周期在弹窗内显示原因并保留输入，页面显示操作结果和空列表说明；配置与订阅页仅禁用命令栏父容器，保留各按钮的选中项绑定。新增条目自动选中，刷新时使用新列表中的行对象，避免值相等的旧对象滞留。未更新显示占位符，成功时间按本地短日期格式显示，七种语言均补齐文案。

第十二批 18 项目 Release x64 构建零警告/错误，相关 **528 / 528** 及主程序 **3063 / 3063** 回归通过且无跳过，格式检查通过。订阅、按钮状态及时间显示仍需该批候选实机复测。

`ccea7ce19a0c0722e5b42d1d57ca167a72881e93` 节点修复候选已在服务器构建并正常安装：安装器 SHA-256 `E95B9F3F7AED5DE1C49914DC783A915BDB5A67E4F085A4B4A7686381AB73A3B6`，MSIX `7091D31C4CE8C1980B58A3EE35EAF535EB43AC3EBD44E700B7F763616FA42481`。实际安装程序集与包内哈希相同，均为 `07E8AE70BB84FC3E80F83A5E19E1C8143CDF040AB530C9F987108E5297F05CBE`。旧 1924 正常卸载后确认包、服务、六处产品目录和产品安装信任已清除，系统代理关闭、地址不存在。由于候选仍为相同包版本，此过程记录为卸载后全新安装，不能当作跨版本升级通过。

## 节点持久化复验与首页读竞争修复

`ccea7ce` 安装后经界面导入已有有效备份，设定 GLOBAL 为 Acceptance HTTP、规则组为 DIRECT。全局模式的 HTTP/SOCKS5 请求实际走测试代理；打开 TUN 后测试地址的透明请求走同一代理，关闭 TUN 后 HTTP/SOCKS5 仍走代理。两组选择在两次内核所有者切换及设置页正常重启后均保留。切换规则模式后，首页显示 DIRECT、请求实际直连；待命显示 DIRECT、系统代理撤销。普通刷新和“刷新运行时”均不改变选中值。六份 `network-ccea-*.json` 记录了请求结果、内核 PID、系统代理与 TUN 网卡/路由。这证明原先的节点显示及 TUN 切换丢失选择缺陷已修复；跨配置隔离、节点删除及异常恢复组合仍须继续。

首页仍偶发短暂“不可用”。四个并发读者读取真实已提交配置的回归测试复现：完整性检查以零等待争用事务锁，将另一个只读检查误判为配置修改。新增只读观察互斥，避免正常首页与托盘读取互相产生 Unknown；真正配置事务期间仍立即返回 Unknown，配置哈希、快照和版本验证不变。修复前两项新测试一失败一通过，修复后 143 项状态及运行配置相关测试全部通过；18 项目 Release x64 构建零警告/错误、主程序 3065 / 3065 回归通过且无跳过，完整格式检查通过。此修复尚未安装到服务器，不能算实机状态稳定性通过。

订阅修复提交 `5d904d2` 的[两项 CI](https://github.com/Water-Run/ClashSharp/actions/runs/35814429951) 均通过。服务器第一次打包在时间戳服务连接阶段失败，保留失败日志后，在待命且无系统代理的环境重试后成功，安装器与 MSIX 签名均为 Valid。签名失败产物未安装，也未绕过签名验证。

## 订阅候选界面验收与手动更新修复

`5d904d2` 通过正常卸载/全新安装切换，安装器 SHA-256 为 `4E97FE5EAB37E22598676AEF2DC4B81824B1AA759D91B98E276C24073BA72365`，MSIX 为 `7D5C98551F0CCEBBCE9D5A022EBBCC2BF262D7C38CEB300A7533645A3AC948BB`；安装程序集与包内哈希均为 `A521877E7F1B7434BE05F6CEE655CDB391966669FF82B71A65698C21DE8D32E1`。上一候选卸载后包、服务、六处产品目录及安装信任清除，系统代理关闭。首页读竞争修复 `8ecbb46` 的[两项 CI](https://github.com/Water-Run/ClashSharp/actions/runs/35816124454) 通过，仍待服务器候选复验。

- 空列表有提示且仅“添加”可用。空地址、`file:` 地址提交时，对话框保持打开、错误提示可见且输入不丢失；Escape 取消后选择相关按钮仍禁用。
- 使用 Tab 和 Enter 添加有效订阅，新增行自动选中、显示“订阅已添加”，未更新日期为“—”。检查返回 200，更新成功且选中行保留，日期完整可见。配置页出现新配置，当前配置仍为本机直连默认配置。
- 重命名保存后显示新名称；编辑地址后 Escape 取消保留原地址。401 响应分别在检查和更新时显示失败，选中行保留，没有伪成功提示。删除确认的默认回车执行取消。
- 发现关闭“自动更新”也阻止手动更新。新增真实回环 HTTP 回归先复现失败，再将启用状态限制应用于定时导入；手动请求仍核验当前链接版本和地址。40 项配置目录、订阅调度及页面相关测试通过；最终主程序 3067 / 3067 回归通过、无跳过，18 项目 Release x64 构建零警告/错误，变更文件格式检查通过。定时导入保持跳过关闭自动更新的条目。编辑开关同时改用已有本地化开关文本，避免中文界面出现 On/Off。新修复尚待实机验证。
- 返回的订阅用量及到期元数据尚未保留和展示；列表更新间隔、最后更新时间缺少可见标签。这些是待处理缺口，不能将订阅生命周期整体计为通过。

截至 03:50 UTC 的截图与操作日志共 607 个文件已归档并校验，压缩包 SHA-256 为 `B34ABE3FB56BC9E4B9357666C33E60FC0C7DE33011F99EC433A8C29A6A2D0535`。

## 订阅异常恢复与元数据补全

`5d904d2` 实机追加验证：401、404 检查和更新均失败；无效 YAML、空响应未生成多余配置，原配置及历史文件哈希保持不变。30 秒延迟请求在导航离开后显示“已取消”，重新进入可再次更新；留在页面等待时按 30 秒超时恢复按钮。把同一条目改为 302 重定向地址后，检查 200、更新成功，证明失败后能恢复操作。显式删除该订阅后，对应已导入配置仍保留，当前配置未改变；删除确认的默认回车执行取消。

通过原生保存对话框导出设置和订阅配置，备份 7712 字节，含 `ProfileCatalog.json` 和四份配置/历史文件，SHA-256 为 `5F4CA6E7B62F9EF3C4701F081634DEEFEA7AC985B552E9D61FBCF2DE748DA7F2`。截至 04:39 UTC 的截图与操作日志共 714 个文件，归档 SHA-256 为 `43E5085C5BE79102FE79760065907605BF2C76881C56874202A59D132B68CF96`。验收矩阵现为 39 项通过、19 项部分完成、83 项待测，各项通过均绑定实际候选及证据。

第十五批读取 `Subscription-Userinfo` 中的上传、下载、总量和到期字段，语义参照[固定 mihomo 版本实现](https://github.com/MetaCubeX/mihomo/blob/v1.19.27/adapter/provider/subscription_info.go)。异常、负数、溢出和缺失值保持未知；有效检查不修改成功更新时间，成功配置事务同时持久化元数据，失败保留旧数据，修改地址清除旧数据。列表改为带本地化标签的可换行信息，缺失与明确无到期限制分别显示，避免原先无列名的裸数字。七种语言均补齐。真实回环 HTTP、旧目录兼容、重新加载及数值边界相关 46 项测试通过，主程序 3077 / 3077 无跳过，18 项目 Release x64 构建零警告/错误，完整格式检查通过；此批界面仍待新包实测。

`656418c` 的[两项 CI](https://github.com/Water-Run/ClashSharp/actions/runs/35817217893) 均通过。服务器打包首次停在隐藏控制台的 `Write-Host`，保留日志、清理本次临时构建信任并将所有输出写入文件后成功，未修改打包验证或绕过签名。安装器 SHA-256 为 `B3F67A7EF86A7F31B47DCF495943F57918EBA3508D43B912DF1DDDE6EBC592A0`，MSIX 为 `DCC465E89A30764B2C955C92B80CDA135167538EACC28016027FC88438E64D11`，两者签名 Valid。旧 5d 候选正常卸载后包、服务与六处产品目录清零。

## 修复恢复、订阅元数据与运行中配置验收

`656418c` 实际完成关闭自动更新后的手动导入；待命首页和托盘持续显示 DIRECT，HTTP/SOCKS 探测直连成功，系统代理与 TUN 关闭。通过托盘安全退出后核心进程结束。管理程序修复进行中取消，重新打开同一安装器显示“继续修复”并成功完成；期间打开 `4d9b426` 则明确提示“请使用原安装器继续”，没有放行安装或修复。普通卸载后包、服务和六处产品目录清零。卸载取消点击晚于操作完成，不能计为取消验收通过。

`4d9b426dc9f39e11de294f7809cb4053482b8872` 的[两项 CI](https://github.com/Water-Run/ClashSharp/actions/runs/35819482538) 均通过。服务器安装器 SHA-256 为 `2D8919656EE1435A5F5D91D8AAC00F531BCD4B395FE736E892B4C89066910BB3`，MSIX 为 `D836EC3B941C9F5D21C9E29FC22D4F56646634F5DD2809047FF06DB4F9FFBFF5`，两者签名 Valid。全新安装后的程序集与载荷一致，SHA-256 为 `E8E6A0539AEC92740807DD8DB11D922414E9E5D520A9CF8D1C11921D9FDB6CDA`，仅验收用户注册此包。

经界面恢复 5d 备份后五份文件哈希均匹配。订阅检查显示 3 KB / 1 GB 和到期时间，成功更新时间保持不变；关闭自动更新仍可手动导入。窗口缩至约 864 × 684，列表、按钮及编辑对话框可用。变更地址会清空旧元数据，明确的零值显示为 0 B / 0 B、无到期限制。

核心运行中切换到 Acceptance Manual 后，已应用配置代次变为 4；同一链接更新为第二版后，源配置和运行配置均包含更新后的节点，代次变为 5。取消历史恢复时文件、代次和 PID 均不变；选择旧版恢复后源文件哈希回到 `7465B1D7CBA78BE037094691C21D88D8E9ADA74E9F56C647716D89D09C3686A8`，代次变为 6，节点页显示旧名称，HTTP/SOCKS 探测成功。对应 JSON 和网络结果保存在 `4d9b426-state`。

第十六批发现并修复两个界面/功能缺口：历史项用“1/2”表示数量且恢复缺少结果提示；响应头立即返回、正文延迟的订阅在 39 秒后仍禁用操作，52 秒时离页才取消。取消后源配置、历史、运行代次与 PID 不变。下载现使用覆盖响应头和正文的同一个 30 秒期限，真实回环 HTTP 测试先复现失败，再验证超时与主动取消的区分、旧数据保留和后续成功重试。配置页补齐所有变更与验证的结果反馈，以及带节点/规则标签的多行历史描述，七种语言均提供文案。相关 371 / 371 和主程序 3086 / 3086 回归无跳过，完整格式检查通过；新包实测仍待完成。

截至 05:35 UTC，截图与操作日志共 848 个文件已归档，SHA-256 为 `E12AFF5B7BFC1842DD6114A2BB0AEF027B75C6E673F550E1E2927C17D2CC668F`。矩阵现为 42 项通过、19 项部分完成、80 项待测；订阅慢请求场景因新增正文超时缺陷退回部分完成，历史恢复功能虽已验证，交互反馈仍待新包复验。

## 正文超时、配置反馈与大列表复验

`0872a3c4edb4527af08a99ab866db808ef97bd06` 的[两项 CI](https://github.com/Water-Run/ClashSharp/actions/runs/35823204548) 均通过。18 项目 Release x64 构建零警告、零错误，主程序 3086 / 3086 回归通过且无跳过。服务器安装器 SHA-256 为 `8E1B92A77025A52D2EE9E54DB321E6085D5C950869893DBC81780A5CF3C83D1C`，MSIX 为 `728B6AC38DE55CF0B54EC1A38F93769363124BD6304D2DE5CA12DCED2096B24E`，两者签名 Valid。普通卸载旧候选后全新安装成功，安装程序集与载荷 SHA-256 一致，为 `BF237562C8F6E864E0F6B75B7F68AB878C3A351BEC81C069B6071E298A2369C3`。安装完成后展开详情已无旧的“未安装”检查结果；再次尝试卸载取消仍晚于操作完成，不计为取消通过。

- 经原生界面导入 4d 设置和历史备份，九份文件哈希全部匹配。备份为 12070 字节，SHA-256 `681B25DCBDCC4B70CF987834833C3540A2DCC7FCCFF6154DD1CA00FE1A413A56`。
- 响应头立即返回、正文停顿的订阅在 29.984 秒关闭请求，页面显示失败并恢复操作。源配置、历史、运行代次、核心 PID 和成功更新时间均不变。第二次请求在导航离页后取消，状态为“已取消”；恢复正常地址后更新成功，时间与 6 KB / 1 GB 元数据一并刷新，自动更新仍关闭。
- 历史下拉框显示完整时间、来源和带标签的节点/规则数量。取消恢复不改变文件、代次或 PID；恢复选中的第二版后，页面明确显示成功，源配置哈希为 `EB70789F5BB2A1393C5BC399BF0AAE6836E2A8243B24F509420D67616DC3C74D`，运行配置也包含新节点，代次变为 3，HTTP/SOCKS 在待命下直连成功、系统代理与 TUN 均关闭。
- 无效 YAML 导入有明确失败提示，配置列表、文件、历史、运行状态与核心 PID 均保持不变。验证已选配置显示成功并保留选中行。含中文的长配置名保存成功且换行可读；删除确认默认回车取消，实际删除非当前配置后只移除该配置及其历史，当前运行状态与核心 PID 不变。
- 前一安装版 4d 经原生文件选择器导入含 180 个节点、301 条规则的配置，取消文件选择不生成条目；设为当前配置后节点和规则均加载，End 到达节点 180 和最后 MATCH 规则，反向滚动正常。规则的来源、类型、目标、策略和命中数没有列名，策略列过窄，这是新记录的界面缺陷。

本批源码为规则页补齐七种语言的列名，表头与数据使用一致的比例列宽，为策略保留更多空间；配置重命名的空名称改为弹窗内提示并保留输入。18 项目 Release x64 构建零警告、零错误，537 / 537 项页面、语言与架构回归通过且无跳过，完整格式检查通过。上述新界面修改尚待下一安装候选复验。

截至 06:12 UTC，原始截图与操作日志共 947 个文件已下载并安全解包，归档 SHA-256 为 `D71A7A120C92D3878D3E988782967FB0FDC127841D533F276FBB00B588927CF3`。新状态、取消和失败前后比较、请求时间及网络探测保存于 `0872a3c-state`。矩阵为 **50 项通过、17 项部分完成、74 项待测**；各通过项继续绑定实际候选，不将源码回归计入实机通过数量。

## 规则列与重命名实机复验

`b3ca1724b2bf17e34015592f2d2919435d346385` 在服务器重新构建并全新安装。安装器 SHA-256 为 `31CBB36AA50662CD7E14171FB1575F53A9234344FDF150BE794BB37D6DF77569`，MSIX 为 `3886BBE6E9D6757C4455DD23E52933007E112CCD08F926B7D10BC0513E971649`。已安装 DLL 与载荷均为 `46285F86ED2BB8D5DA2B817FC1C104FC77FD6F4BB7D54A363E7BDF3AF608AD96`；所有签名有效，只有验收账户注册此包。此次仍是卸载后全新安装，不作为升级覆盖。

- 导入 180 节点、301 规则的本地配置后，规则页在最大化与约 878×684 窗口下完整显示来源、类型、目标、策略、命中数，表头与行对齐。End 到达第 300 条及末尾 MATCH；一般目标的悬停提示正常，超长目标仍待单独验收。
- 空名称保存显示“请输入配置名称”，弹窗保持打开。补填 `Large acceptance renamed after validation` 后保存成功，页面反馈、当前配置标签与磁盘目录中的名称一致。配置页小窗口下按钮、长名称和更新时间均可见。
- 前一候选在待命模式删除当前订阅配置后，内置直连成为当前项，源文件与历史删除，订阅保留。HTTP 和使用字面地址的 SOCKS 直连成功；再次更新订阅重建配置，当前内置项和核心 PID 保持不变。规则/全局接管与 TUN 组合仍待覆盖。

截至 06:43 UTC，1007 个截图和操作日志文件已经下载并安全解包，归档 SHA-256 为 `3077B7FF06E0A32DC9DC2291F007A13C46AE16223BFBF28CB5104CF516C1F6FE`。安装及状态回执保存于 `b3ca172-candidate-manifest.json`、`b3ca172-clean-install-verification.json` 和 `b3ca172-state`。[该提交 CI](https://github.com/Water-Run/ClashSharp/actions/runs/35825894773) 两项工作均成功。矩阵为 **50 项通过、20 项部分完成、71 项待测**；新增两项部分完成仅记录已观察的规则顺序和窗口尺寸，不计为完整通过。

## 累计计数读取修复（源码验证）

06:48—06:55 UTC 在 `b3ca172` 的 TUN 规则模式观察到：活动连接存在时列表正常，关闭全部后显示“无法读取 mihomo 连接”，后续连接出现又恢复。核心 PID 保持 12648，运行代次为 3。服务路径此前只接受 JSON 数组；[固定 mihomo 版本的统计实现](https://github.com/MetaCubeX/mihomo/blob/v1.19.27/tunnel/statistic/manager.go) 在没有连接时使用空切片并序列化为 `null`，累计上传和下载独立于连接列表保留。

读取层现接受空连接列表，并在服务 IPC 中携带核心进程的计数周期及累计上传/下载。首页速率和本次流量改用这些计数的增量；计数周期变化时不会把旧核心基线从新核心扣除，失败读取不推进基线，多消费者仍共享采样。18 项目 Release x64 构建零警告、零错误，110 项定向回归及主程序 3100 / 3100 项回归通过，完整格式检查通过。新逻辑尚未安装到服务器；数据库采样、停机前收尾及分类统计仍需继续处理，流量缺陷保持未关闭。

数据库采样随后接入同一核心累计计数。计数周期、最后计数、当前连接基线与统计增量使用同一 SQLite 事务提交，重新打开仓库或重试不会重复入账；总计、日期与配置统计包含已经结束的短连接。规则命中改为每个观察到的连接只计一次，已有节点分类仍来自观察到的连接，不能用于补全未观察连接的分类。清空历史保留必要计数基线，避免下一次采样回填已清空数据。计划中的核心停止、重启及所有者切换先执行最多两秒的收尾采样；核心已崩溃或控制器不可用时只能保留最后成功记录，收尾失败会记录稳定诊断码并继续释放网络。

持久化与收尾相关 140 项回归通过；最终主程序 **3113 / 3113** 项通过且无跳过，18 项目 Release x64 构建零警告、零错误，完整格式验证通过。真实下载后对照界面、SQLite 和核心累计计数、采样开关组合、计划退出及新安装包复验仍未完成。

## 触发器输入丢失（实机发现、待新包复验）

07:16—07:24 UTC 在 `b3ca172` 小窗口中复现：清空名称后直接点保存，仍保存默认名称；编辑已有项，输入 `Acceptance entered name` 后直接点保存仍保留旧名称。对照操作先按 Tab 移出文本框，再保存 `Acceptance name after Tab`，新名称才出现在列表。原始截图分别为 `071650Z-123582990`、`071659Z-668467621`、`072300Z-457465924`、`072316Z-215832159`、`072347Z-275923980` 和 `072439Z-512098012`，均使用本次日期及标准截图文件名前缀。

触发器页的名称、阈值、窗口秒数、运行秒数、目标时间和通知文本改为随文本变化更新编辑模型，避免保存按钮先读取到失焦前的值。空列表和全部已启用/停用时，对应批量按钮不再可点击。18 项目 Release x64 构建零警告、零错误，35 项现有触发器编辑及页面架构回归通过，完整格式检查通过；真实输入、校验和保存仍需在包含修复的新包中复验，暂不计为实机通过。

## 流量累计与退出收尾实机验证

`9dfbd3917905dc644ad9bb9bf715c122c813b6b5` 在服务器构建、正常卸载前版并全新安装，签名有效。安装器 SHA-256 `10915E4B6A3B54597726A5ED12F8D85D2B24810D99B6F6F665601CF8E70BCA75`，MSIX `B60E84CFD32CF60B7C9FBB4B23E821BF295AE3AFF38FFBA316BBE0CF9F2FF18B`，已安装 DLL 与载荷一致，为 `666BC1F13A10619A67DF9DF4CBFF526B7A07266A8D881C28EA2640FDE2C7C512`。[CI](https://github.com/Water-Run/ClashSharp/actions/runs/35830707546) 通过。

- App 核心待命、透明代理关闭：45,677 字节的固定 YAML 在约 2 毫秒内下载完成，采样时已经没有活动连接。核心计数与 SQLite 总计、配置累计均为上传 89、下载 45,846 字节，包含 HTTP 开销；连接明细为零，证明短连接不会再从总计中漏掉。
- 随后下载固定 245,760 字节正文，耗时 60.026 秒。完成后核心、SQLite 与统计页总计一致：上传 172、下载 292,287 字节。规则 MATCH 命中只增加一次，节点分类仍为采样观察到的部分流量，需继续明确界面含义。
- 再次短下载后数据库仍为 172 / 292,287，07:33:38 UTC 切换“未启用”时补写为 261 / 338,133，核心停止且界面速率归零。启动新核心、再下载后托盘安全退出，在 07:34:34 UTC 补写最终 89 / 45,846；累计变为 350 / 383,979。重新启动后该累计保留，没有重复入账。
- 服务 TUN 规则模式的核心在零连接时实际返回 `connections: null`，连接页正常显示“当前没有活动连接”。导入专用配置后使用 `--noproxy '*'` 向验收地址发起请求，服务端确认 `viaProxy: true`，45,677 字节正文下载成功；新核心计数至少为 180 / 46,122，随后包含系统后台流量的累计在停止时写入数据库。此项不作为纯下载正文的逐字节对账。

非敏感计数、前后数据库摘要和安装回执已下载到 `9dfbd39-state`。截至 07:32 UTC 的 1052 个截图与操作日志归档 SHA-256 为 `111BC32DD11D3714DF076C8C99BB2F5E8B795374F23A15F0FC96E39F85C36C84`，两端校验一致。矩阵为 **51 项通过、20 项部分完成、70 项待测**；流量采样开关、清空历史和分类展示尚未完成，总流量场景保持部分完成。

## 触发器输入、持久化与真实执行

`6c38c1ef937908cee4a88ee10a52470b17b25490` 已在服务器全新安装，已安装 DLL 与载荷哈希一致。空名称、重复名称、空通知正文及负流量阈值均保留编辑器并提示错误；48 字符名称、指定通知正文和阈值 12,345 直接用鼠标点保存后正确写入数据库，不再要求先按 Tab。时间、时长输入还需逐项复验。

08:06 UTC 重启后，启用的 `Acceptance notification` 进入程序任务仅执行一次，执行及 outbox 均为成功，尝试次数为一；另一禁用任务未触发。列表显示最近触发时间，Windows 通知中心实际显示 `Acceptance notification body 0746`。依据为 `6c38c1e-state/trigger-after-restart.json`、截图 `080729Z-470551018` 与 `081016Z-177502232`。数据和安装回执归档 SHA-256 为 `9AE2742E4140952AB05A85E166E9C8AB44DB135AAED12237EC0D02869ABC9F56`，两端一致。

本轮又发现并处理以下交互问题：

- 全部启用后数据库两项均为启用，列表第一项仍显示关闭；离开再进入才恢复。`d4d94f0` 保持条目身份并发布属性更新，防止列表重建留下错误开关显示。
- 新建滚动窗口流量及重开已有本次运行流量时，统计范围有值但标题为空。四类参数下拉框改为绑定实际选项对象。
- 条件与行为选择器没有选择、甚至搜索无结果时，“添加”仍可关闭弹窗。现在仅选中一项时允许提交。

以上源码已通过 18 项目构建（零警告、零错误）、3,115 项主程序测试及完整格式检查；[CI 35835357695](https://github.com/Water-Run/ClashSharp/actions/runs/35835357695) 通过。`d4d94f0a5e4864d5a30c773718bc8d31598e3277` 于 08:14 UTC 服务器打包成功，正常卸载前版后全新安装；卸载核对包、服务、六个产品目录均为零，用户数据目录不存在。新安装器 SHA-256 为 `04AAA3E68DD2B59113A032DEFC0EA539CE4BE23E9365CE673D8DB8296F150FE6`，MSIX 为 `D8E6B6DE50DA7B491B55C452471A2D27D09F57456428208873A136B0A0229BE8`；签名有效，已安装 DLL 与载荷一致，为 `1C4C3DED19F90321BA20EB89AE9C07877D2BEE64C874E6BBC2024321D6844444`。首次启动前从关闭状态备份复制验收触发器数据库作为测试夹具，不计为产品升级或备份导入验收。

实机复验确认两个批量按钮和单项开关即时显示正确，四类参数下拉框初始标题可见；无选择及搜索无结果时“添加”禁用，Enter 不误关闭，选中后可正常提交。60.5 秒直接保存后 SQLite 为 `00:01:00.5000000`。条件添加、删除、上移及行为添加、上移的顺序与参数均核对落库；确认删除第二条任务后，第一条任务、条件、行为不受影响。回执归档 `d4d94f0-receipts-0834.zip` SHA-256 为 `D6DBCA5F1F7DA9FF6C226AD4B045BB1A0345FE6547E6D30138AFF4910DA6AC91`。截至 08:13 UTC 的 1,164 个截图与轨迹文件归档为 `8B89CF2B159C0508B084F13428484DA7F2DD62972ACEA6BCE2385BFD894EA03D`；两端均已校验。后续截图仍持续采集。

另有两个未修复问题：成功执行后触发器分类日志仍为空（现有桥接仅记录调度器异常），以及空触发器日志刷新后分类标题变空。矩阵为 **56 项通过、26 项部分完成、59 项待测**。时间与运行时长输入、行为移除、实际运行组合等仍需继续；没有实际界面的克隆及手动运行不作为已实现能力。

## 未完成覆盖与证据

第一批源码验证：18 项目 Release x64 构建零警告、零错误；完整格式检查 1548 文件、零变更。四组本机回归共 **5238 / 5238**（主程序 2977、Installer Core 1022、Presentation 143、普通 Windows 1096），均无跳过。首次全量检查指出两处仍期待旧 SVG/磁贴布局的界面断言，按新行为更新后主程序整组重跑通过。27 项需要隔离环境的证书及原生目录测试留给隔离运行；首次过滤遗漏了其中 7 项目录测试，它们被环境前置条件拒绝，未作为产品失败或通过计入最终数字。

离开连接页时正常取消流量订阅被记录为 `connections-page-stream` 意外错误的问题已在第二批源码修复，仍需对新包验证离开、重新进入和真实网络中断的区分。

全局接管、TUN、全部设置及持久化、订阅完整生命周期、触发器完整生命周期、配置历史、搜索与日志清理、备份导入和重置、多语言及 RTL、缩放和键盘、托盘与退出恢复、安装修复、取消恢复、卸载及自动目录清理、临时验收资源回收均需继续逐项完成。已扫描页面静态交互控件 115 项，仍需补入代码创建的对话框、菜单和跨功能组合；扫描数量不是通过数量。

原始截图、操作轨迹、控制项清单、问题列表、网络结果、构建及测试日志保存在忽略目录 `artifacts/verification/server-acceptance-20260922` 与目标验收目录。截图文件名保留 UTC 时间。临时签名密钥与账户只用于验收，完成后单独清理并保留非敏感证据。
